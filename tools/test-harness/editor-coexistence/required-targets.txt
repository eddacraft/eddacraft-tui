# Targets that must be present on a CI runner for the editor-coexistence
# gate to count as meaningful. The harness fails when the count of present
# targets drops below `threshold`.
#
# Format: one target name per non-comment, non-blank line. Threshold is the
# minimum count; targets above the threshold may skip without failing CI.
#
# Bumping the threshold requires:
#   1. The new target's runner exists under targets/<name>.sh
#   2. The CI workflow installs it (see .github/workflows/editor-coexistence.yml)
#   3. The matrix table in docs/policies/editor-coexistence.md lists it.

threshold = 4

rust-analyzer
tsserver
pyright
ruff
prettier
eslint
