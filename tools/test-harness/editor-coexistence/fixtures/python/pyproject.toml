[project]
name = "editor-coexistence-fixture-python"
version = "0.0.0"
requires-python = ">=3.9"

[tool.pyright]
include = ["module.py"]
typeCheckingMode = "basic"

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F"]
