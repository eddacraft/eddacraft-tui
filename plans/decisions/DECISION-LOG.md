# Decision Log

Condensed index of all Architecture Decision Records (ADRs). Read the linked
document for full context and trade-off analysis.

For the ADR process and when to write one, see
[docs/guides/adr-process.md](../../docs/guides/adr-process.md). For a live
integrity report against this directory (no duplicate numbers, no missing
entries, next available ADR number) run `pnpm adr:check`. The fixture tests
for the script itself live at `pnpm test:adr-integrity`.

## Core Philosophy

These define how Anvil behaves by default. All features must align.

| ADR | Decision | Status |
|-----|----------|--------|
| [001](001-planless-first.md) | Deliver value without requiring plans or config; codebase is the baseline truth | Accepted |
| [002](002-warnings-over-blocks.md) | Warnings don't block (exit 0) by default; opt-in `fail-on-warnings` for enforcement | Accepted |
| [003](003-new-edges-only.md) | Baseline existing architecture on first run; only warn on new violations | Accepted |
| [004](004-suppression-syntax.md) | `@anvil-ignore[-until DATE] WARNING-ID: reason` for targeted, explained suppressions | Accepted |

## Technology Stack

| ADR | Decision | Status |
|-----|----------|--------|
| [000](000-use-zod-for-aps-schema-definition.md) | Zod for APS schema definition, export to JSON Schema when needed | Accepted |
| [005](005-ink-over-opentui.md) | Ink (React-based Node.js) for TUI components | Accepted |
| [006](006-hybrid-dc-opa.md) | Dependency-Cruiser for static analysis + OPA for policy evaluation | Accepted (engine half amended by ADR-040) |
| [007](007-pulumi-iac.md) | Pulumi (TypeScript) for infrastructure as code | Accepted |
| [014](014-language-allocation-tree-ts-vs-rust.md) | TypeScript for orchestration/UX; Rust for CPU-bound hot paths | Proposed |
| [051](051-cli-panic-unwind-for-untrusted-input.md) | `anvil` CLI builds with `panic = "unwind"` (was `abort`) so untrusted-input panics (e.g. a `regorus` internal panic during `anvil policy eval`) unwind to a `catch_unwind` guard at the policy-engine facade → structured error + non-zero exit, not a no-diagnostics abort. Generalises the `release-napi` FFI-boundary discipline to the CLI | Accepted |

## Rust Migration

| ADR | Decision | Status |
|-----|----------|--------|
| [011a](011a-rust-core-engine.md) | Rust core engine for performance-critical paths (watch mode, analysis, policy) | Superseded |
| [012](012-rust-cli-replacement.md) | Single `anvil` Rust binary replaces Node.js CLI; big bang, no hybrid period | Accepted |
| [017](017-crates-io-naming.md) | Publish crates under `eddacraft-anvil-*` namespace to avoid collisions | Accepted |
| [026](026-rust-scanner-authoritative.md) | Rust scanner is authoritative; `patterns/compiled/registry.json` is the contract; TS scanner stays only for in-process IDE/MCP surfaces until a napi-rs migration retires it | Accepted (amended by ADR-033) |
| [033](033-park-ide-mcp-retire-ts-scanner.md) | Archive VSCode extension and TS MCP server (now in sibling `eddacraft/anvil-archive` at `anvil-archive/anvil-vscode-extension/`, `anvil-archive/anvil-mcp-server/`); archive TS scanner, TS suppression parser, and parity harness to `anvil-archive/anvil-ts-scanner/`; napi crate stays as build canary; surfaces return as new active packages via DRVR / RMCPF / future ADR | Proposed |
| [040](040-rust-policy-engine-regorus.md) | Adopt `regorus` as the embedded Rust policy engine behind `crates/anvil-policy-engine` facade; amends ADR-006 engine half (Dependency-Cruiser unaffected); POLENG-008 bench gate validates parity vs. Go OPA reference | Accepted |

## Product and Distribution

| ADR | Decision | Status |
|-----|----------|--------|
| [106](106-agent-integration-registry-and-managed-installers.md) | One typed agent registry with independent skill/MCP capabilities, managed embedded skill installation, and evidence-constrained MCP adapters | Accepted |
| [018](018-product-ip-architecture.md) | Free base tier, source-proprietary model; three foundational repos as OSS | Accepted |
| [020](020-versioning-strategy.md) | Lockstep versioning for Anvil core; independent versioning for separate products, OSS, and peripherals | Accepted |
| [025](025-package-manager-distribution.md) | Package manager distribution strategy across npm / crates.io / GitHub Releases | Accepted |
| [045](045-update-signing-scheme.md) | Minisign (Ed25519 + BLAKE2b) signs release artefacts; pure-Rust `minisign-verify` enforces the chain inside `anvil update`; long-lived key custody via GitHub Actions secret + offline backup | Proposed |
| [047](047-eddacraft-tui-canonical-source-mirror.md) | Move `eddacraft-tui` canonical source back into Anvil; keep the public repo as a read-only mirror and crates.io as external distribution | Accepted |
| [050](050-eddacraft-tui-runner-and-cli-policy.md) | `eddacraft-tui` ships an opt-in `runner` feature flag as a small fallback CLI shell: `launch_cli(cli)` with minimal `lexopt` parser, global flags (`--help`, `--version`, `--theme`, `--no-tui`, `--config`), first-level subcommand dispatch, config-path handoff, typed mode/theme hints, and consumer-owned lifecycle/render-loop/command semantics; single-app `launch_default(app)` may remain as a future narrow adapter. Anvil keeps its own `TerminalGuard` + `clap` tree (non-adopter); no `clap` in core, no `[[bin]]` in core, no sibling crate | Accepted |
| [054](054-json-render-tui-engine-home.md) | json-render TUI engine (spec parser, registry trait, tree walker, generic base catalogue) lives in `eddacraft-tui` behind a `json-render` feature; Anvil-domain catalogue + `.anvil/` data context + dashboard surface live in `anvil-tui`. No standalone `anvil-tui-render` crate; one-way dep `anvil-tui → eddacraft-tui`. Re-homes TUIDASH | Proposed |
| [115](115-eddacraft-tui-surface-trait-evolution.md) | Retain the defaulted `Surface::text_entry_active` hook, classify additions to the stable downstream-implemented `Surface` trait as compatibility hazards, and publish the change in `eddacraft-tui` 0.5.0 rather than a patch release | Proposed |
| [116](116-kindling-product-profiles-and-governance-record.md) | One kindling product has standalone-memory and embedded air-gapped anvil-governance profiles inside the single shipped `anvil` binary. Upstream `generic-event@1` (planned `kindling-runtime` 0.4.0 floor) carries the Rust-authoritative, closed `anvil.governance.v1` inventory; legacy `gate.evaluated` maps to canonical `gate_evaluated` only during migration/dual-read. Anvil selectively retains governance outcomes with authenticated explicit-prune-only retention, non-prunable `governance_pruned` receipts, hard store/repository/payload/low-disk ceilings, durable store-or-bounded-spool admission, and a preallocated crash-durable `recording_gap` ledger; usage rolls for 30 days and the outage spool is capped at 7 days/64 MiB. ADR-036's tracked project UUID plus a subordinate, owner-registered local worktree ID forbid current-working-directory fallback and accidental cross-root attribution; aggregate recording gaps declare unknown scope and run instead of inventing identity, and the OS account remains the stated operator trust boundary. Legacy sidecars migrate through an atomic dry-run-first state machine, full-row digest parity, generation-owned pre-cutover rollback, one release of dual-read, no dual-write, and forward-only recovery after local writer cutover; its payload-free manifest is verification-only, and prune stays blocked until cleanup removes source and backup copies. KFIT-007 builds the default-off sink, KFIT-009 owns local cutover, and KFIT-011 owns per-profile release-default selection plus immediate TypeScript integration-package removal after generated contracts replace it; legacy-state profiles without a marker remain legacy-only until explicit migration. Runtime/transport remain in `anvil-cli`, intercept traits stay transport-free, and DPO retains dashboard ownership. ADR-116 amends ADR-035/089 retention wording, preserves ADR-088 taxonomy and ADR-089 privacy/egress, supersedes ADR-088's dotted/KDS/upstream-kind mechanism and ADR-089's ordinal product-kind model, and leaves ADR-064 unchanged. | Accepted 2026-08-03 (operator) |
| [117](117-repo-state-checks-are-not-per-pr-gates.md) | A merge gate may only fail for something a commit did — assessed **per assertion, not per surface**. `check-release-plan.mjs` makes three assertions; only the third (active window names an already-published git tag) can go red with no commit, because creating a tag flips it. That one moves behind a default-off `--shipped-window-check` flag: `docs:check` omits it, so `Docs Lint` still enforces the two pure-content assertions (exactly one `## Active window`; no legacy/`Shipped` headers). The tag comparison gains the enforcement it does not have today — `pnpm release-plan:check` has **no caller** (declared at `package.json:75`, absent from `preflight.sh`'s gate list, and `closeout.sh` never reads `RELEASE-PLAN.md`) — via a `preflight.sh` `run_gate` entry plus a non-required `Repo Health` workflow on push-to-`main` and a schedule; verifying the prune inside `closeout.sh` is a follow-up. `adr`, `aps` and `index-freshness` stay gating unchanged: each fails only when a commit broke it. The duplicate `aps` execution is dropped, leaving `APS Drift Check` as the single required APS gate. **No** sub-product path carve-out is created — it has no motivating case once the tag assertion is off the gate. Required-context list on the ruleset is untouched. Prompted by `v0.9.2-beta` being tagged, which failed every open PR without anyone editing a file and blocked a security-relevant acknowledgements-kit patch (ATTRIB-026) behind an unrelated product's release plan | Proposed |
| [055](055-aps-oss-carveout.md) | Narrow carve-out from ADR-018: read-only consumers of the public APS format — the `anvil plan dashboard` viewer AND the `scripts/aps/*` format tooling (drift-check, index-count derivation, active-lint) — may be published `Apache-2.0` in `anvil-plan-spec` as one-way forks, bounded by a three-test rule (public-format consumer, public-OSS-only deps, no product internals); product engines and product dashboards stay closed. Legal gate cleared 2026-06-18; carve-out now in effect | Accepted |
| [056](056-format-flag-output-selector.md) | `--format <auto\|tui\|plain\|json\|sarif>` value-enum is the canonical output selector; `OutputMode` gains a `Sarif` variant with explicit precedence; `--json`/`--no-tui` retained as compatibility aliases (no deprecation); `--format sarif` valid only on finding-emitting commands and never TTY-auto-selected. Future machine formats are added as enum values, not booleans. **Amended 2026-05-29:** scoped per-command on `check`/`gate`/`audit` (not global) — `--format` already collides with `export`/`validate`'s domain flags | Accepted |
| [060](060-anvil-home-install-root-override.md) | Per-project state resolution under `ANVIL_HOME` (gates DISTRIB-006): re-root install/user-owned state (user dir, daemon socket/PID, cache/logs) under the prefix; keep per-project `.anvil/` (baseline/cache/witness) + `anvil/project-id` resolving to the **project root** (Option a, not re-rooted) so candidate tests run against the real repo with witness continuity — but **guard** durable project-state mutations (baseline/witness/cutoff) behind `--touch-project-state`, read-only/dry-run by default under a non-default `ANVIL_HOME`; `status --json` gains `installRoot` + write-gated fields; unset = byte-for-byte default. Rejected Option (b) (re-root project discovery) as defeating the side-by-side purpose. Cross-version chain *format* compat stays DISTRIB-005/`anvil migrate` | Accepted |
| [066](066-github-device-flow-cli-auth.md) | Adopt the **GitHub Device Authorization Grant** (RFC 8628) as the default `anvil auth login`, **brokered server-side through `anvil-api`**, replacing the broken homegrown device-code browser-confirm flow (`/device/confirm` was hardened by #1779 to require an `Authorization` header the website cannot send, so device login is un-completable). The CLI talks only to `anvil-api`; `anvil-api` calls `github.com/login/device/code` + the device-token poll endpoint. Brokering exists for **identity-brokering + server-side licence mint / active-status gate**, NOT secret custody — the device grant is a **public-client** flow (no `client_secret`). New dedicated "Anvil CLI" OAuth app (separate from `eddacraft Docs`, Device Flow ticked) + Key Vault `github-cli-client-id` / `github-cli-client-secret`; new versioned `POST /api/v1/auth/github-device/{start,poll}` (old `/device/*` retained until the new CLI ships); account-link by GitHub numeric `github_id` (first-link of an email-invited record via **any verified** GitHub email, then `github_id` authoritative); invitation stays **email-keyed** with GitHub as a linked auth method — funnel unchanged, invite email drops the retired activate URL/code and points at `anvil auth login`/`--otp`, vestigial interactive `/admin/invite` device code removed (`tokenOnly` unaffected); DB-backed `github_device_sessions` (Vercel serverless); single-use atomic `DELETE … RETURNING` mint, hashed `poll_token`/`device_code` at rest, GitHub token revoked after `fetchGitHubUser`. Reuses `auth-github.ts` mint path (extract `mintLicenceForGitHubUser`) + the ES256 `signLicence` (no licence schema change — `LicenceClaims` already supports `provider:'github'`). Email OTP (`--otp`) retained as the email-proof fallback. Activation page tombstoned + admin-invite path rebuilt. Supersedes BAUTH's device-confirm sub-flow; reuses DOCSAUTH's GitHub OAuth path. Rejected: fix the website-confirm flow (needs a website auth surface + a browser); CLI talks to GitHub directly (licence mint/gate/signing key cannot move client-side) | Accepted 2026-06-04 |
| [081](081-anvil-admin-login-github-identity.md) | Fold **admin auth onto the GitHub identity**: a new `anvil admin login` runs the ADR-066 device flow against a **dedicated admin start/poll surface**, gated on a **staff allowlist keyed on `github_id`** (a nullable `github_id` column on `admin_keys`, Pulumi-provisioned/audited like keys), and mints a **separate, short-lived admin-scoped licence** (`roles:["admin"]`, ~12–24h TTL, rotating refresh) stored in its own `admin-credentials.json` (0600) — **not** the everyday `credentials.json`. `admin-auth.ts` gains a third path: a valid ES256 admin licence whose `github_id` is **re-checked against the allowlist per request** (claim alone insufficient), so allowlist removal revokes even an unexpired licence; `adminActor` = the GitHub identity email, `adminAuthMethod = 'github_identity'`. The static shared/per-operator key stays as the **CI / break-glass** path (additive, non-breaking). Supersedes CIB-070 for human operators. Rejected: **(B)** an `admin` scope on the everyday user licence (biggest blast radius — every daily session would be admin); **(C)** device-flow *delivers the static key* into config (server stores only the hash, so it re-introduces a long-lived on-disk secret with no auto-expiry). Open: TTL + destructive-action step-up; `admin_keys.github_id` vs a dedicated `admin_identities` table; claim-plus-allowlist vs claim-only; RBAC alignment with ADR-076's deferred staff axis | Proposed |
| [103](103-tty-default-activation-tui.md) | TTY-default activation TUI for `anvil start`, staged behind an opt-in first release (`--tui` / `ANVIL_ACTIVATION_TUI=1`) before the default flip. Trust boundary: only genuine interactive terminals enter the TUI; `--verify`, `--json`, piped/CI, `--no-tui`, and `ANVIL_NO_TUI=1` stay deterministic plain/machine contracts. Consent chrome is shared and unticked by default (CIB-165), suppressed under gated `ANVIL_HOME`, and reused by downstream WOW-005/006 gates per PR #3231. ACTTUI-007 contract matrix + ACTTUI-008 welcome convergence gate the default flip | Accepted 2026-07-25 (owner) |
| [114](114-bare-anvil-ensure-surface.md) | Bare `anvil` is the daily **ensure** surface (idempotent daemon + spine + existing MCP ensure only); `anvil start` remains activate/reconfigure/reinstall with consent. Bare never installs NotPresent MCP/workflows/hooks or re-offers declined picks; recovery names `start`. Supersedes CIB-177 bare-always-exit-2 for the ensure path; first-run stays honest. Implementation: ONSW; conductor: JOURNEY-011. **Accepted 2026-08-01** for `v0.10.0-beta` (default-on; config-Absent = never-activated; global `--json` compact; exit 0/1) | Accepted 2026-08-01 |
| [104](104-dashboard-host-server-module-boundary.md) | Dashboard host/server/module boundary: DASH moves to dedicated `apps/dashboard` (React + Vite 8 + TanStack Router/Query/Table + shadcn/ui + Tailwind v4, json-render first); Wave 1 introduces `crates/anvil-dashboard-server` as the loopback-bound read-only local API over kernel capabilities, with OpenAPI -> generated TS client -> TanStack Query as the seam. Dashboard modules are UI adapters over kernel capabilities and may request actions, but kernel/server code owns permissions, workflow state, audit, evidence, and policy decisions. Wave 1 includes Protection Overview (user-facing) plus Plan Driver (APS dogfood); Better Auth, Monaco, xterm.js, writes, and real-time channels are deferred. | Accepted 2026-07-09 (owner, planning council) |
| [107](107-fleet-telemetry-consent-posture.md) | Fleet telemetry consent posture (FLEET design gate): **disclosed opt-out** beacon for beta+ builds — first-run notice strictly before the first send; hard offs `anvil telemetry off` / `ANVIL_TELEMETRY=off` / `DO_NOT_TRACK=1`. Identity = anonymous random per-install UUID (rotatable, salted principal never on the wire). Payload = enumerated allowlist only (schema_version, install_id, version, install_method, platform, channel, flag snapshot version, feature-key usage counts per the FLAGS-design contract); paths/repos/args/hostnames/emails forbidden, IPs not retained, allowlist changes need a dated ADR amendment. Emission ≤1/24h, async fire-and-forget, zero blocking latency; `anvil telemetry` shows the exact next payload; ingest = versioned `apps/anvil-api` route (raw 90d, aggregates kept); beacon is the ONLY remote usage emission (licence-gate/auth stay clean; ADR-035 pipes unchanged). Motivation: investor-grade evidence (installs, retention, version/install mix, feature adoption) — OQ3 resolved 2026-07-15. Rejected: opt-in (biased single-digit sample), zero telemetry (no retention proof), licence-gate piggyback (covert channel), third-party analytics SDK **Amended 2026-08-11:** complementary BACT pipe for identity-bound beta CS (login stamps / later feature touch) — FLEET identity rules unchanged; no `install_id`→account join on the beacon. | Accepted 2026-07-15 (operator); amended 2026-08-11 |

## Configuration and Structure

| ADR | Decision | Status |
|-----|----------|--------|
| [016](016-unified-config-format.md) | Consolidate three config files into single TOML with source delegation | Proposed |
| [046](046-yaml-parser-migration-deferral.md) | Defer `serde_yaml` → maintained-parser migration; byte-level pre-pass (size cap, alias reject, depth cap) is the actual defence; review 2026-08-15 | Accepted |
| [021](021-in-house-nx-rust-plugin.md) | In-house `@eddacraft/nx-rust` plugin; reject monodon (no licence) and cargo-make (not a substitute) (originally drafted as ADR-026; renumbered in DOCGOV-004 to resolve a duplicate-number conflict). Amended 2026-05-29 (DEVENV-003): plugin extracted to the standalone public repo `eddacraft/nxrust`, consumed from the registry as `@eddacraft/nxrust`; the in-repo `tools/nx-rust` vendored copy was dead code and removed | Proposed |
| [099](099-moon-build-system-migration.md) | moon (moonrepo) build-system migration: **do not migrate now — defer to a time-boxed, Rust-crates-only go/no-go spike** running moon alongside nx, gated on hard criteria (remote-cache parity incl. an accepted moonbase/self-hosted story vs the lost `@nx/azure-cache`; proto toolchain coexistence with `.nvmrc`/`rust-toolchain.toml`/ADR-057 `CARGO_TARGET_DIR`; ADR-049 cross-language seam as moon deps without the PR #1729 cascade; a named `@eddacraft/anvil-generators` path). Captures the full nx→moon mapping; native Rust support would retire the ADR-021 in-house plugin. Does **not** supersede ADR-021/049/057; a **go** amendment would. changesets + cargo-dist stay runner-independent | Proposed |
| [100](100-committed-exception-store-provenance.md) | L4 gate reads the exception store from the **tip of the pushed range's tree**, not the worktree: suppression authority must be committed to count (a grant at the tip covers earlier commits in the same push; tip-without-store = empty, fail-safe; legacy `.anvil/exceptions.json` never influences gates — migrate + commit to count). Principle: **configuration may be local; authority must be committed** — `anvil/policy.yml` loading deliberately unchanged. Closes the 2026-07-04 council uncommitted-self-grant finding (EXCEPT-010) | Accepted |
| [023](023-shared-packages-restructure.md) | Retire `packages/platform/`, consolidate into `packages/shared/` | Proposed |
| [044](044-mcp-entry-activation-owned.md) | `mcpServers.anvil` MCP entries are owned by the activation flow; backend swaps overwrite in place on next `anvil start` with a one-line notice; `--keep-mcp` opts out; protocol-shape changes still route through `anvil migrate`. Amended 2026-07-11 (CIB-184, following CIB-165/ACTTUI-009): interactive picker candidates start unticked — Enter with nothing ticked writes no editor config; non-interactive auto policy and `UnsafeDrift` refusal unchanged | Proposed |
| [092](092-mcp-optional-activation-spine.md) | MCP-optional activation spine: daemon ensure → worktree registration → hooks → save-time armed is required; MCP is optional L0; no indefinite `ready_restart_required` when spine is live; `--no-mcp` / `ANVIL_NO_MCP` corporate opt-out (ACTMO; closes #2937 class) | Accepted 2026-06-26 (operator) |
| [113](113-mcp-2026-07-28-dual-era-and-rmcp.md) | Dual-era MCP `2026-07-28` stdio support: modern discovery and per-request metadata alongside four sealed legacy initialise-era versions; typed internal adapter retained because `rmcp 3.0.0` cannot enforce anvil's four-MiB frame ceiling; MCP26-012 owns bounded SDK adoption; domain handlers stay anvil-owned. | Accepted 2026-07-29 |
| [101](101-headless-save-time-driver.md) | Headless background save-time driver (DSV-046, planning-council direction validate). **Keystone:** daemon-supervised detached `anvil watch --save-time-driver` sidecars (one per durable registration), not in-daemon `notify` (ADR-064 parser boundary). `SaveTimeDriverSupervisor` consumes ACTMO-014 `membership_hook`; PID registry under `{ANVIL_HOME}/runtime/save-time-drivers/`; wire `save_time_driver: attached|absent|failed` on `WorktreeStatusV1`; opt-out `ANVIL_NO_SAVE_TIME_DRIVER`. Splits DSV-047..051. Rejected: in-daemon notify, copy downgrade, foreground watch from `start`. Closes RELEASE-PLAN usefulness addendum design gate | Accepted 2026-07-04 (operator) |
| [094](094-worktree-registration-ux.md) | Worktree registration UX (ACTMO-013, planning-council hardened). **Keystone:** durable registration is a daemon-side **persisted, TTL-exempt, reload-on-start** set (not a CLI heartbeat, not the 30s session lease) with reaper + distinct-worktree cap — reuses the `session.register` RPC but changes daemon semantics (retracts "no wire change"). Explicit surface is `anvil workspace register [PATH]` / `unregister` (not `anvil intercept`; makes the config-only command a live RPC client); client classifies daemon errors (`WorktreeAlreadyOwned`→heartbeat existing owner, server-authoritative canonicalisation via `dunce`). `anvil start` outside a registerable worktree (via `git rev-parse`; bare/`.git`-internal rejected) ensures the daemon, registers nothing, reports honestly (exit 0). Global opt-in `register --all` bounded to **exact** confinement-allowlist entries (prefix skipped+reported; never a scan); the **persistent** `register_on_start: [paths]` is a **separate additive `workspace.yaml` key** (NOT a field on `deny_unknown_fields` `AllowEntry`, which would fail an older daemon **closed** and collapse the confinement trust floor) + format-version, deferred to ACTMO-019. Status = two axes (membership vs assurance); DSV-046 driver subscribes to a registry membership-change signal. New-worktree auto-reg = `anvil workspace install-hook` (portable git alias) design-gated for Worktrunk; local app = scoped daemon-control surface only, deferred past the v0.9.0-beta cut. Splits into ACTMO-014..021. | Accepted 2026-06-29 (operator) |
| [049](049-cross-language-build-contract.md) | Cross-language `^build` contract: defer to nxrust D-009 (binding upstream); Anvil enforces the seam at the script layer (`pnpm test:js && pnpm test:rust`, PR #1729) until adopting nxrust generators that enforce at the generator boundary; no parallel Anvil recipe | Accepted |
| [057](057-dev-environment-hardening.md) | Local dev-environment hardening for the concurrent-agent box: layered Rust target relocation (committed `.cargo/config.toml` floor on `/home` + per-worktree `CARGO_TARGET_DIR` override), `[profile.dev]` debuginfo trim, nx-rust executor `CARGO_TARGET_DIR`-awareness + `.anvil-building` sentinel, disk-pressure eviction timer, Node version alignment, and `.config/wt.toml` worktree-lifecycle + parity fixes (wave 1); reproducible base (mise/devcontainer/Nix) + nx-cache/sccache dedup deferred to a go/no-go spike (DEVENV). Council `plan-6b3be127` | Accepted |
| [102](102-architecture-cli-surface.md) | Architecture CLI surface (ARCHCFG-006 design gate): resolve the eight documented-but-unbuilt `anvil architecture` commands — **build** `init` (non-interactive scaffold, `--template layered\|hexagonal`) and `visualise` (Mermaid renderer over the existing `show` parse); **redirect** `check` → `anvil gate --only-checks architecture`, `watch` → `anvil watch --action none\|gate`, baseline flags → `anvil baseline`/ADR-039; **reject** `list` (synonym of `show`); **defer** `impact`/`export`/`debug` behind the ARCHCFG-015 usage gate. Principle: `anvil architecture` stays a thin config-authoring surface (author/validate/render); analysis, watching, baselining, and impact live in their existing homes; the guide documents only shipped commands. **Amended 2026-07-06:** `impact` reframed as a config dry-run diff (`--file <proposed.yaml>`, current-vs-proposed violation sets — not a graph-tools projection, `--rule` DSL dropped); `export` ownership question recorded (collides with the existing top-level `anvil export`); `debug` shape recorded (`show --layer --files`, a flag not a subcommand); deferral re-entry owned by ARCHCFG-015 (opens only after `init`+`visualise` ship and a concrete usage signal names a deferred capability) | Proposed |
| [058](058-sarif-shared-emitter-no-finding-model.md) | SARIF output uses a thin shared emitter (`output/sarif.rs`) owning the pinned 2.1.0 document subset, fed by per-command adapters (SARIFOUT-003/004/005) that map each command's existing finding shape; deliberately **no** unified in-process finding model and **no** engine-crate refactor (SARIF itself is the shared target). Bundled upstream 2.1.0 schema, verbatim (no fork), is the validation gate. Second SARIFOUT candidate ADR alongside ADR-056 | Accepted |

## Intercept and Enforcement

| ADR | Decision | Status |
|-----|----------|--------|
| [015](015-intercept-loop-enforcement.md) | Rust daemon detects file changes from AI agents, evaluates policy, interrupts sessions | Accepted (ratified 2026-07-04 via ADR-098) |
| [031](031-validation-latency-rubric.md) | Shared latency measurement rubric for intercept validation modes; standardises modes, timing boundaries, dimensions, and warm p95 SLOs so INTD / DRVR / RTAI cite one source | Proposed |
| [036](036-daemon-scope-discovery-and-boundaries.md) | Daemon scope, discovery, and OS-boundary policy: what the intercept daemon is allowed to see, watch, and act on | Accepted |
| [038](038-hook-surface-and-noise-discipline.md) | Hook surface contract and noise-discipline rules for the intercept hook system | Accepted |
| [043](043-ssh-remote-host-daemon.md) | SSH remote support runs Anvil on the remote host; local surfaces are display/control only and must not claim local daemon protection for remote files | Proposed |
| [061](061-save-time-daemon-delta-validation.md) | Save-time governance is daemon-mediated delta validation: `anvil watch` stops cold-spawning `check --all` per save and instead routes changed paths to the existing intercept daemon (`anvil/validate_paths` alongside `scan_buffer`) over warm Graph V2 hot-read state; watch/MCP/intercept become thin clients of one per-host daemon (one warm model, one work budget, per-host rayon); whole-repo scan becomes explicit/background with a `clean\|stale\|pending\|running\|unavailable` workspace-assurance state; daemon-absent degrades to a scoped (never `--all`) subprocess / in-process scan reporting `unavailable`, exit 0. Phase 1 (scope per-save check, RLB-007) ships independently of daemon/GV2. Sequences INTD/DRVR/RLB/GV2 behind one product contract | Accepted 2026-06-01 (council `plan-5768ae0c`) |
| [097](097-allowlist-admits-explicit-entries-only.md) | Amends ADR-061 §7: **`allowlist` confinement admits only explicit operator allow entries — no implicit primary**. The §7 rule "the primary check-in root is implicitly admitted" was a same-uid bypass (CIB-149): a connection's "primary" was a client-declared wire value (first `validate_paths.workspace_root`, then `RegisterSession.worktree`), so a same-uid peer could self-admit any unlisted root by naming it. Ratifies PR #3117's fail-closed removal (`to_admitted_roots()` takes no path arg; `verified_primary`/`is_allowlist`/`seed_save_time_verified_primary` removed; `set_originating_session` telemetry-only); empty allow list admits nothing; `open` mode unchanged. Correct **permanent** design per §7's own trust boundary (same-uid is the only real boundary ⇒ no worktree is daemon-attestable; `/proc/<pid>/cwd` already rejected), confirmed by the CIB-149 post-merge council (4 personas). Amends only the confinement clause; rest of ADR-061 stands. Rejected: restore implicit primary from a "verified" source (infeasible under same-uid), revert #3117 (reopens the bypass) | Proposed |
| [082](082-daemon-lifecycle-user-startup.md) | User-facing daemon lifecycle layer with a **tiered** startup mode: `anvil start` auto-starts the per-user daemon (opt-out `--no-daemon`); `anvil watch` prompts in TTY and falls back to the scoped check in headless/`--json`/CI/MCP/hook contexts. Makes daemon-backed protection the normal path while preserving explicit opt-out and non-interactive safety. Supersedes ADR-079; narrows the ADR-075 no-auto-start invariant for the `start` activation moment | Accepted 2026-06-15 (operator) |
| [083](083-gctx-mcp-delivery-target.md) | GCTX-002: MCP target decision for assistant graph context delivery (Rust RMCPF `anvil mcp serve` primary; additive tools/resources; TS path parked per ADR-033). Entry gate for v0.9.0-beta per ADR-075. | Accepted 2026-06-15 |
| [084](084-gctx-graph-handle-access.md) | GCTX graph-handle access: `anvil mcp serve` queries the running daemon over a new read-only `anvil/gctx/*` RPC (daemon-required, degrade via `WorkspaceAssurance`/CE-7); **daemon-side** egress projection (RPC response IS the sealed DTO); two new crates — `anvil-gctx-types` (graph-free value DTOs + CE-5 no-leak test, shared by proto+MCP) and `anvil-gctx-egress` (daemon-only `GctxProjector`); separate `GctxDispatch`, not the save-time path. Thin slice = `anvil_search_symbols` (GCTX-010); Phase 2 = a same-process second GCTX service surface over the **same** graphs (option A) with a lock-free read snapshot (arc-swap), isolating query-serving from the hot path — not a second graph. Binding conditions C1–C5 gate GCTX-010 → Ready. | Accepted 2026-06-15 |
| [085](085-daemon-full-scan-executor.md) | Daemon **full-scan executor** (DSV-045): `anvil/request_full_scan` today only sets `Pending` — no loop drives `Pending→Running→Clean` or populates `KernelGraphCache`, so a fresh MCP session stays cold until a save (breaks ADR-084 C1). Add a background-pool executor over the DSV-006 primitives (`walk_capped`/`run_chunked_scan`/`ScanCancel`/`DosCaps`/two-pool scheduler): spawn on `Pending`, drive `start_scan`→walk+parse+`apply_delta`→`complete_scan`, with `ScanCancel` preemption (interactive `validate_paths` wins), a `scan_timeout` watchdog, and eviction-rewarm on `WarmStateEvicted`. Parser feed reuses the injected `SymbolParser` on `scheduler.background()` (not interactive); daemon still links no parser (ADR-064/067). No-parser ⇒ `mark_stale`, never phantom-`Clean`. Per-key dirty-during-scan flag: an `apply_delta` mid-`Running` ⇒ `complete_scan` marks `Stale(CrossFileResolutionNeeded)`+re-queue. **New `AssuranceState::Bounded` wire variant** (+`#[serde(other)]` fallback so it is genuinely additive) + a `scan_coverage` field on `WorkspaceAssurance` (gitignore pre-filter, then over-`max_walk_files` ⇒ `Bounded`, never `Clean`; GCTX maps to identity-results-marked-bounded). Lock held only for `start_scan`/`complete_scan` (ADR-084 C2); executor opens its own `WorkspaceAnchor`, `read_rel`. Files fed as `ChangeKind::Create`; order-independent convergence (GV2-011) validated against a save-driven baseline. Per-key `scan-enqueued` CAS coalesces repeated `request_full_scan`. Reactive to `Pending` + first-contact auto-enqueue; GCTX-010 adds session-init/on-demand triggers. DLIFE soft dep (UX docs only); restart rides CE-7 `Stale`/`NotReady`; SLO via the existing `ipc_roundtrip` bench (scan-in-flight scenario, ADR-031 80ms p95). Rejected: fold-into-GCTX, minimal-no-cancel, truncation-as-silent-`Clean`; epoch-token deferred. | Accepted 2026-06-16 (council `plan-898d9222`) |
| [086](086-symbol-call-graph-substrate.md) | Save-time symbol-level **call-graph substrate** (GCALL-001), the gating model for the GCALL module that unblocks `anvil_find_callers` (GCTX-014). The warm graph has no call edges today (`EdgeType::Calls` exists but is never emitted; `FileSymbols` carries only symbols/imports/reexports). Mirror the dependency-graph machine one level finer — **caller `SymbolIdentity` → callee `SymbolIdentity`** edges: (1) a fourth `#[serde(default)] calls: Vec<CallSite>` channel on `FileSymbols` (`CallSite { from: LocalSymbolRef, callee: CalleeRef{name, via_import}, line }`), file-local + unresolved; caller = nearest enclosing named symbol, else a synthetic file-scope Module node (`module_scope` flag); extractor MUST derive caller ordinals via `for_file_symbols` over the emitted `symbols` slice; (2) **lift-time callee resolution** through a **distinct symbol-granular `re_resolve_calls`** (reuses the `resolve_import` module hop + accumulator pattern + the `dependents_of(callee)` affected-set locality, NOT `re_resolve_imports`' file-level `first()` source), `CalleeRef{name=export-name, via_import}` (alias reverse-map, namespace member, barrel-follow capped at `MAX_REEXPORT_HOPS`, default-export = Unresolved), emitting resident `EdgeType::Calls` edges through `GraphDelta` — extractor stays per-file/parse-only; (3) **best-effort static** model: `Resolved`/`AmbiguousOverload`(fan-out capped at `MAX_OVERLOAD_FANOUT`, results `heuristic:true`)/`Unresolved`(drop + `partial`-mark), cycles/recursion first-class, terminated read-side; (4) read API `callers_of(target, depth)` = a `collect_dependents`-shaped bounded BFS over incoming `Calls` edges (seen-set + GV2-026 depth clamp + node budget + sorted frontiers), identity-only. Budget: extraction additive to the existing single parse (no double parse), lift incremental over `dependents_of`, hot certify path unchanged (calls are consumer-only); stays in the ADR-031 80ms save-time p95 (GCALL-006 gate) bounded by a per-file `MAX_CALL_SITES` cap (over-cap ⇒ truncate + `partial`), never a silent breach; cold warming rides the ADR-085 executor (normal path, not a fallback). Egress identity-only by default through the single CE-5 `GctxProjector` choke point; snippets a gated CE-1 escalation; GCALL-007 (PV-9-modelled) ratifies. Rejected: extractor-side resolution, type-aware overload/dynamic-dispatch resolution, a separate `CallGraph` store, silent-drop of unresolved calls. Adversarial-reviewed (caller attribution, ordinal coherence, callee-resolution patterns, fan-out cap, re-resolution scope/O-bound, no-double-parse). | Accepted 2026-06-17 (operator) |
| [091](091-gctx-cursor-fingerprint-integrity.md) | GCTX pagination cursor (CIB-103): **keep the reproducible FNV-1a filter fingerprint; do not add an HMAC/keyed construction now.** The opaque keyset cursor (`{q: fnv1a(filters), k: last SymbolIdentity}`, plaintext hex) is a server-minted *seek position*, **not** an authorisation/capability token — a follow-up page is re-authorised by the echoed query filters and the CE-5 identity-only projection choke point, not by the cursor. Forgery only reseeks within the caller's own already-authorised query (no source via CE-5, no cross-query leak, bounded by `MAX_CURSOR_BYTES`), so HMAC would add per-daemon key material/rotation + a wire change to defend a non-threat. Documented as non-security-bearing + pinned by a forged-cursor test. **Binding revisit trigger:** flip to a keyed MAC (or server-held opaque state) the moment a cursor encodes snippet/source payload (Phase-2 CE-1), cross-tenant/trust-scope state, or any result not fully re-authorised by the echoed filters. Rejected: HMAC-now (over-engineering ahead of the trigger), server-side cursor state (memory/eviction-expiry not warranted for identity-only paging). | Accepted 2026-06-24 (owner; council `council-f8ed314e` ratified) |
| [095](095-gctx-cli-secondary-surface.md) | GCTX read-surface consumers: **MCP-first; the CLI is a co-equal *secondary* thin client over the same `anvil/gctx/*` RPC, not a runtime fallback.** Structural basis: MCP tools and any CLI command are peers over one daemon spine (both call `daemon_rpc_call` → `GctxProjector`), so a CLI "fallback" rescues only host/transport failures, never daemon-down or cold-graph (both return `Unavailable`/`NotReady` over the same socket; no embedded graph fallback exists). CLI is positioned as the **primary surface for out-of-session consumers** (CI, hooks, humans, non-MCP agents) + a manual escape hatch for MCP-not-wired hosts; **agent-triggered MCP→CLI auto-fallback is an owner-accepted residual** (B2 — not a preventable non-goal: the CLI can't distinguish an agent's `Bash` call from a human's, so it's bounded by B1's enumerable-verb egress gate rather than pretended closed; also resource-worst per-call spawn). Build off proto DTOs + `daemon_rpc_call`, NOT the MCP tool `call()` funcs (they enforce a remote-client `workspaceRoot` boundary); emit raw DTO via `--json`, skip the MCP envelope + `redact_workspace_root`; no new crate. v1 = read-only, identity-only default, CI-shaped verbs (`affected_tests`/`impact_of_change`) **behind an experimental gate** until the CI contract lands: distinct exit codes for `NotReady`/`Unavailable`/`Disabled` vs `Ready`-with-empty (cold-graph `NotReady` has no `tests` field → naive `jq` reads it as "0 affected" and passes a gate — a false-negative correctness defect), a CI-safe daemon warm entrypoint (`ensure_daemon`, ADR-082 forbids non-interactive auto-spawn), and a bounded `--wait` for warm-up (ADR-085/DSV-045). **Egress ceiling (§6, owner-settled B1):** the process-local `GRAPH_EGRESS_SPENT` ceiling doesn't exist for a session-less CLI caller, but v1's two bounded-report verbs don't carry the reconstruct-the-graph risk it guards against (a same-uid caller already has repo-wide filesystem read); `dos.rs`'s `RpsBucket` is ruled out as the fix (per-connection DoS budget, not a confidentiality-volume one). Before `search_symbols`/`find_callers`/`find_dependents` reach the CLI, add a daemon-side, uid-keyed, persistent egress counter instead. **Pagination (§7, owner-settled):** those same three verbs use ADR-091 keyset cursors — auto-page to exhaustion inside the command (the `anvil kindling` `collect_daemon_rows`/`MAX_PAGES` shape), never expose a raw `--cursor` flag; same promotion gate as the egress counter. Resource model: daemon holds the graph in both paths (identical); delta is client-lifecycle only (~10 MB persistent MCP vs ~2 ms/query CLI spawn) → complementary by call frequency. Applies the ADR-092 daemon-spine/MCP-optional posture at the read-surface consumption layer. Four-persona planning council interrogated; five-persona ratification council RATIFY-WITH-AMENDMENTS (folded in). **B1/B2 owner-settled 2026-07-02.** | Accepted 2026-07-02 (owner) |
| [096](096-diagnostic-severity-category-forward-compat.md) | Diagnostic wire enums `Severity`/`Category` (`anvil.diagnostic.v1`) become **forward-compatible** via a `#[serde(other)] Unknown` unit variant, implementing the envelope spec's existing MUST ("subscribers MUST treat unknown `severity` values as `warning`, unknown `category` values as `other` … rather than dropping") which was previously unimplemented — the closed enums failed the whole `Diagnostic` parse on an unknown value (pin test `diagnostic_schema_unknown_severity_value_fails`), the opposite of the spec + the lone strict outlier among the wire enums (`Mode` `Unknown(String)`, `StaleReason`/`AssuranceState`/`MirrorPath` `serde(other)`). Consumers treat `Unknown` as warning/other (single-sourced in `midedit_severity_class`; 6 exhaustive `Severity` match sites gained an arm, `Category` had none). Chose the unit `serde(other)` variant over `Unknown(String)` (keeps `Copy` on two pervasively value-matched enums; matches ADR-085/`MirrorPath` precedent) and over folding into `Warning`/`Other` (keeps the unknown value visible to telemetry). Stays on `v1` (additive direction the spec blesses). Resolves envelope-spec Open Question #1 (category open at the consumer). Rejected: stay-strict + amend-spec-to-drop-the-MUST (discards a promised guarantee; diagnostics vanish on older consumers). Closes #2243. | Accepted 2026-07-02 (owner) |
| [105](105-shared-base-graph-persistence.md) | **ADR-069 successor — shared base-graph persistence (storage layout only).** Replace ADR-069's per-`WorktreeKey` snapshot (O(worktrees) on disk, orphan-prone CIB-092c, blocking `ANVIL_PERSIST_GRAPH` default-on) with **one write-once, content-addressed base per repo per merge-base commit + live per-worktree overlays**. Base = the full graph of the **merge-base commit's committed tree**, read via git objects (`ls-tree -r` + `cat-file --batch`, the `l4_engine.rs` pattern — **zero new deps**) and parsed by tree-sitter in a **CLI subprocess** — NEVER a working tree, NEVER the resident daemon (ADR-061/064 upheld; ADR-067 injection precedent). Magic `ANVILGB1` (per-worktree keeps `ANVILGC1`); `SnapshotPayload` DTO reused **verbatim** (no header-kind byte — would shift `HEADER_LEN` + break goldens); one `SNAPSHOT_FORMAT_VERSION`/`SNAPSHOT_BACKING_SCHEMA_VERSION` covers both classes, `schema_epoch` bump invalidates both. Warm-start = load base by replay + apply overlay (ADR-085 executor scoped to changed-vs-base files) into one materialised petgraph; base shared **on disk only** (shared-RAM deferred). Disjoint ids: base owns `[0, base_next_id)`, overlay above a reserved watermark, cross-boundary imports re-resolved at compose time; **COMBINED-STATE golden** = base+scripted-overlay == cold scan. **Trust line unchanged from ADR-069** (restore indexes never the verdict; comes up stale; content-hash-authoritative reconcile before certify) — storage layout only, explicitly **NOT** event-sourced persist-and-trust delta-replay. **Amends ADR-069 §5/§10:** single-flight via `O_EXCL` `.producing/<sha>.lock` `{pid,start_time}` (PID-reuse guard; reclaim iff pid dead OR mtime > 2× p95, via `O_EXCL` rename + atomic swap); GC = in-daemon refcount over live worktrees' merge-bases, keep-set = ACTMO durable registration (ADR-094), respects active claims — reclaims the **new shared-base orphan class** this ADR introduces (a merge-base-keyed analogue of, not a replacement for, the per-worktree `<hash>.root` sweep; the historic per-worktree orphan race was already closed by CIB-096). Trigger: directory-level inotify (`IN_MOVED_TO\|IN_CREATE`) on refs/packed-refs/HEADs (≤4 fds/repo on the existing budget; ENOSPC ⇒ CLI check-and-request; ~500ms debounce; ref updates are rename-based). Merge-base keyed on default branch (`origin/HEAD`/configured), `@{upstream}` refinement-only; unresolvable ⇒ cold. Production = detached `anvil graph-base build --merge-base <sha>`; pool thread claim+spawn+enqueue only, dedicated reaper owns `child.wait()` (background pool floors at 1 — blocking forbidden); newer sha cancels-and-restarts via ScanCancel, cap N=3/lineage ⇒ cold+log+ADR-090 envelope+re-arm on quiescence; all failure non-fatal via ADR-090 worktree-scoped envelopes. **Zero new crates:** key-agnostic I/O seam = named submodule `anvil_intercept::snapshot_io::store` (write_sealed/load_sealed/dirfd+openat2/size-caps only; per-worktree `.root`/sweep stays private) shipped as its own **no-behaviour-diff PR first** (byte-identical golden gate) before base code; producer = `graph_base_producer` module in anvil-cli (daemon_dep_boundary green); re-entrant `persistence_route` module → `Base{merge_base_sha}`\|`PerWorktree{canonical_root}` + structured `persistence.route{route,reason}` events. Old per-worktree path **kept permanently** for uncovered topologies (detached HEAD, no merge-base, no default branch); inherits the identical ADR-070 `cfg(unix)` Windows gap (not new). Epoch-mismatch base ⇒ ignored (cold) + GC-eligible once unreferenced at old epoch — discard-and-rebuild, never migrate, never mixed-epoch compose (protects disjoint-id invariant). Graduation gate (successor-specific): corrupt-base incident rate, herd-miss under single-flight, warm-start latency budget + standing correctness/GC — default-on flip is the LAST item. Rejected: clean-worktree promotion (dirty-poisoning), CI/server bases (off-trunk misses), nearest-ancestor fallback (deferred), shared-RAM/COW base (ADR-031 latency, deferred), no-lock idempotent production, `@{upstream}` keying (upstream-less locals), owner-worktree writer election, event-sourced delta-replay (per ADR-069), two new crates (`anvil-snapshot-store`/`anvil-graph-base`). Synthesised by planning council `plan-89a47ac7`. | Accepted 2026-07-11 |
| [109](109-lsp-agent-integration-reconsidered.md) | Narrows ADR-083's Alternatives Considered table: its "no capability negotiation or resource model" line, as applied to a rejected non-MCP channel, doesn't hold for LSP specifically (LSP has its own capability-negotiation handshake) — the other two stated objections (second transport, no shared agent-tool story) are untouched. Doesn't reopen GCTX-002 (MCP delivery target for graph-context stands); reciprocal amendment note added to ADR-083. **Protocol plurality:** MCP and LSP are both industry standards, and anvil's strategy is to meet people and agents where they already work rather than consolidate on one — un-parks RTAI-005's (`anvil lsp`) mid-edit diagnostics surface **in principle** (build LSP alongside MCP, not instead of it), on a live prospect conversation specifically about LSP (independently drawing a ReSharper-capability comparison) satisfying RTAI-005's "concrete demand signal" trigger; broader ecosystem LSP-adoption trend (opencode, Claude Code, Copilot app) recorded as directional context, not the trigger itself. Scheduling/spike-sizing remains open, owned by the operator; the ReSharper comparison's broader find-usages/impact-analysis capabilities still belong to the separate, unscoped graph-backed-navigation idea, not RTAI-005's diagnostics-only scope | Accepted 2026-07-16 (operator) |
| [111](111-graph-backed-lsp-references.md) | Exact graph-backed LSP references are a separate LSPNAV/GCTX capability, not an RTAI-005 expansion. V1 certifies one language against a closed occurrence taxonomy and advertises references only through dynamic document-scoped registration. One immutable composite graph snapshot, response-set-wide anchored verification, closed no-partial CE-5 outcomes, immutable lowerable ceilings, exclusive save-time capacity, hierarchical daemon-derived work credits, CE-11 rollback, and a six-stage evidence-gated delivery sequence govern the surface. Impact-of-change and affected-test projection remain later Proposed intents without v1 wire contracts. Planning Council `plan-33b005f5`. | Proposed |
| [112](112-antipattern-gate-severity-and-generated-files.md) | Anti-pattern gate severity reconciliation + generated-file handling. A beta tester's `anvil gate` failed CI on TanStack Router's committed `routeTree.gen.ts` because `run_check_antipattern` hardcodes the anti-pattern `severity_threshold` to `Warning` (the only override site; both MCP paths pass `Error`), promoting 27 of 44 warning-severity rules — including weak-crypto, unsafe-rendering, and JWT-`none` — to CI-blocking (exit 2), contrary to ADR-002 (warnings-over-blocks; `fail-on-warnings` opt-in). **Decision:** (1) immediate — exclude machine-generated files from the anti-pattern scan by path/banner signal, deliberately orthogonal to the `eslint-disable`/`@ts-nocheck` markers AP-001 detects so it cannot double as a gate bypass (proposed in PR #3373); (2) promote the genuinely must-block security rules to `error`; (3) drop the `Warning` override so warnings-over-blocks holds per ADR-002, exposing a `fail-on-warnings` opt-in with a migration note (never a silent default flip); (4) defer anti-pattern finding-baselining ("new edges only") to its own ADR-gated module. Reconciles ADR-002; CIB-199; five-persona Council decision review 2026-07-21 | Accepted 2026-07-21 (owner) |

## Policy and Governance

| ADR | Decision | Status |
|-----|----------|--------|
| [108](108-policy-authoring-lint-and-agent-guidance.md) | Customer agents may cognitively author Rego through a small shipped skill, while Anvil owns deterministic target-aware lint, validation, tests, evaluation, and enforcement; comprehensive agent guidance is generated, embedded, routed on demand through CLI/MCP, and excluded from ambient context and the public docs build | Accepted 2026-07-16 (owner) |
| [019](019-flags-observability-alignment.md) | Align feature flag telemetry with OBS/Kindling before FLAGS work | Proposed |
| [022](022-opa-agent-orchestration.md) | OPA Agent orchestration for continuous policy intent translation and explainable guidance | Proposed |
| [035](035-three-pipe-observability-rule.md) | Three-pipe observability rule: Kindling = governance facts, Notification = user-visible state, tracing/OTEL = ephemeral debugging (never source-of-truth); `traceparent` is the cross-pipe correlation key. ADR-116 amends Kindling's write-once wording to append-only normal operation with one authenticated, explicit, receipted governance-prune exception. | Accepted; retention wording amended 2026-08-03 by ADR-116 |
| [090](090-daemon-worktree-scoped-health-envelopes.md) | Daemon-originated, worktree-scoped health envelopes (CIB-098): the INTD-015 fan-out denies any envelope with no `originating_session_id` (the cross-session privacy guard), so daemon-internal health notifications (the ADR-069 §10 persist-failure signal) are never delivered. Add a distinct, **explicitly-flagged** daemon-health envelope class carrying no session-scoped content + `correlation.worktree`; the fan-out authorizes it by **worktree** (deliver only to subscribers owning a session bound to that worktree, via a new `is_authorised_for_worktree`), Deny otherwise. The no-session Deny is unchanged for all session-scoped envelopes; cross-worktree privacy preserved. Rejected: status/metrics pull-only (no push), broadcast-to-all (cross-worktree path leak) | Accepted 2026-06-23 |
| [037](037-witness-chain-and-l4-policy.md) | Witness chain and L4 policy framework for cross-surface policy evidence | Accepted |
| [039](039-baseline-policy-and-hard-pinned-classes.md) | Baseline policy and hard-pinned rule classes; codifies which warning classes never get baselined | Accepted |
| [041](041-flag-snapshot-usage-join-contract.md) | Usage rows store resolved flag context inline; manifest `key` is the stable join key; ADR-019 stays gate-affecting-only for standalone Kindling flag facts | Accepted |
| [048](048-feature-group-architectural-model.md) | Feature Group is a defaults carrier (class + audiences + lifecycle) with per-flag override; hybrid taxonomy (`primaryGroup` surface + `tags` capability); kill-switch is a universal runtime channel via `FlagOverrides.emergency`, not a per-group class | Accepted |
| [052](052-automated-drift-snapshots.md) | Capture drift as an append-only **edge-delta event ledger** (`anvil/drift/edges.ndjson`, carrying `anvil_version` + `rules_sha`) appended on merge-to-`main` via the PR that introduces the edges — not periodic whole-state snapshots. Event-driven (no intra-week blindness), lossless, supports net + peak, team-shared + trunk-safe with no scheduler/extra-PR/bypass. Consumed by `anvil drift report` + INSIGHTS-003. Supersedes the original scheduled-CI-snapshot proposal; rejects daemon/hook/witness-chain/orphan-branch (planning council `plan-0e9c300c`) | Proposed |
| [059](059-production-tracing-sink.md) | Production tracing sink: OTLP-neutral (OTel) instrumentation → **Azure Monitor + Application Insights** (via the Azure Monitor OTel exporter), **dashboards hand-rolled (KQL/Workbook) first, Azure Managed Grafana later**. Exports **operator-hosted surfaces only** (`apps/anvil-api`) while the local-first Rust CLI/daemon stay formatter + local-file (never auto-export); redaction-wrapped (TRACE-003), config-gated off by default; alerting via uncapped Azure Monitor rules. Chosen for Azure stack consolidation over Honeycomb (best trace-debug query, but separate vendor + ~2-trigger free cap) and Grafana Cloud. Feature-flagged usage stays Kindling-of-record (USAGE), App Insights breadcrumbs only. Resolves EXPORT OQ1 | Accepted |
| [062](062-policy-evidence-drift-as-evidence.md) | Policy & evidence source-quality drift as first-class **advisory** evidence: stale / superseded / conflicting / unverified cited sources become deterministic (`AsOf`-injected), severity-graded `evidence_drift_findings` that **downgrade `evidence_strength`** (error→weak, warning→moderate) instead of blocking (ADR-002). Fields land on the CEWS `EvidenceRecord` (`policy_source_ref`, `policy_source_digest`, `policy_canonical`, `policy_review_due`, `policy_superseded_by`, `source_conflict_status`, `evidence_verification_status`, `code_anchor_status`); computation in MDGOV M2; defers to ADR-058 (no unified finding model — maps to SARIF per-command); clean-room from the DocGraph reference (MIT, no dependency); `code.*` beta-later via graph-v2-foundation | Proposed |
| [063](063-gv2-hot-path-boundary.md) | Graph V2 **hot-/non-hot-path read boundary** for save-time validation: a read is hot-path-admissible (inside `validate_paths` / driver mid-edit, against the ADR-031 budget) **iff** answerable from **resident warm indexes** in O(1)/O(bounded-fan-out) with no parse, no cross-file resolution, no transitive traversal beyond **1-hop reverse-impact**, and no I/O. Allowlist = resident extract lookup, known-edge existence, 1-hop `dependents_of()`, precomputed arch-index checks; everything else (parse/resolve/>1-hop/scan/rebuild/persist) is background-only. A warm miss returns a typed `stale` → `StaleReason` → fallback and **never** escalates on the hot path. Reverse-impact **depth is a hard-capped, feature-flagged lever** (default 1 hop, switchable to 2 without re-coding). One rule across INTD+DRVR+GV2 surfaces, behind the frozen ADR-061 wire; enforced by a GV2-022 type split + the ADR-031 benchmark. Closes the ADR-061 §9 boundary gate; clears GV2-022 freeze + sub-phase A′ | Accepted |
| [064](064-intercept-graph-cache-crate-boundary.md) | Resolves council blocker **B5** (predecessor to B1) for daemon save-time sub-phase A: extract a new internal crate **`eddacraft-anvil-graph-cache`** (`SymbolGraph`, `DependencyGraph`, incremental apply-delta, net-new `certify`) that both `anvil-kernel` and `anvil-intercept` depend on, instead of adding `anvil-kernel` to the daemon's deps. Cycle audit: kernel does **not** depend on intercept, so neither option cycles — the deciding factor is build-weight + the documented `watcher.rs:28` refusal. The graph layer is **already parse-free** (`update_file` takes parsed `FileSymbols`; only the relocatable plain structs `ImportEdge`/`FileSymbols` couple it to the parser — moved to `anvil-kernel-types::graph` beside `SymbolNode`). New crate deps are `petgraph`-only (already in-tree); **no `tree-sitter`/parser/`notify`/`walkdir`** enters the resident daemon. Parsing stays kernel-side; the daemon hot path only **reads** (`certify` over `dependents_of`, net-new — zero prod callers). Unblocks B1's `(SymbolGraph, DependencyGraph)` cache + `certify(sym, dep, change, delta, budget)` signature; gives sub-phase A′ GV2 hot-read its parser-free home. Rejected: add `anvil-kernel` to intercept deps (drags the parser surface into the daemon, reverses `watcher.rs:28`); host in `anvil-kernel-types` (turns the minimal type crate into a logic crate) | Accepted |
| [067](067-daemon-symbol-feed-parse-hook.md) | Resolves ADR-064's "Task 7/8 must nail" wiring detail for DSV-005: the daemon obtains parsed `FileSymbols` through a **dependency-inverted parse hook** (EIP **Content Enricher behind a Messaging Gateway**), not the spec's async watcher feed. `anvil-intercept` defines the `SymbolParser` trait (links no parser); `validate_paths` enriches the change it holds by parsing the **exact** openat2-guarded bytes it hashed (`fed_symbols(path, bytes)`) — no second read, so no B2 stale-symbol race. The tree-sitter `KernelSymbolParser` lives in `anvil-cli` (which deps both kernel + daemon) and is injected via `ForegroundOpts::with_symbol_parser`, so tree-sitter links into the **binary**, never the `anvil-intercept` crate (`daemon_dep_boundary` stays green) — ADR-064 honoured, not revisited. Async watcher feed reframed as a future *advisory cache-warmer*, never the attestation source. Interim path-stable (FNV-1a) symbol-id base; a collision degrades safely to `Partial` (never false `Certified`); GV2 (A′) supersedes it. Rejected: async watcher store as verdict source (B2 race); add a kernel dep to the daemon (reverses ADR-064) | Accepted 2026-06-03 (design council) |
| [068](068-windows-save-time-read-safety.md) | Resolves DSV-010's open Windows read-safety risk: the save-time verdict's Unix guard (`openat2(RESOLVE_NO_SYMLINKS \| RESOLVE_BENEATH)` against a held `O_PATH` dirfd, `path_safety.rs`) has no Windows analog. Mirror it structurally with **`NtCreateFile` anchored at a held workspace directory handle + `OBJ_DONT_REPARSE`** (per-component `FILE_OPEN_REPARSE_POINT` ladder fallback) — preserving C2 (held-handle identity; post-admission retarget can't redirect), no-reparse traversal (symlinks **and** junctions), beneath-root, and B2 (read-then-certify the exact bytes; refuse oversized, never truncate). Lives in `anvil-intercept-win32` (the FFI isolation crate) exposed as a safe `read_under`; `anvil-intercept` stays `forbid(unsafe_code)`. Adds a Windows-hardened structural normaliser (backslash/drive/UNC/device-prefix/ADS/trailing-dot/reserved-name rejection) beyond the Unix `normalise_rel`. Precedent: Go `os.Root` (golang/go#73080). Rejected: Win32-only `CreateFileW` + `GetFinalPathNameByHandle` verify-beneath (no handle-relative open ⇒ loses C2; verify-after-open not deny-during-resolve) — kept only as optional defence-in-depth; a documented weaker Windows guarantee (asymmetric posture + DSV-009 parity divergence); deferring Windows read-safety. Peer-SID auth check is a separate DSV-010 prerequisite, not this ADR | Accepted 2026-06-04 |
| [069](069-graph-v2-persistence.md) | Graph V2 persistence (GV2-021, daemon save-time **sub-phase B**): persist the warm graph indexes as a **sealed canonical-DTO serde snapshot** (`postcard`) — **not** an embedded DB (`rusqlite`), **not** zero-copy mmap (`rkyv`), **not** a self-describing codec (CBOR) — because the artefact is **load-once, disposable cache state** (read whole at warm-start into resident indexes, never queried from disk, discarded on any doubt), which removes every advantage the heavier stores exist for while they'd add native/`unsafe` weight the lean daemon (ADR-061/064) refuses. **Serialization (design-council 2026-06-04):** the live `SymbolGraph`/`DependencyGraph` are NOT serde-serializable (no derives; petgraph `serde-1` off; `NodeIndex` is an ephemeral slot), so persist a sealed `SnapshotPayload` DTO keyed by the semantic `u64` symbol-id (EIP Canonical Data Model + Message Translator); load replays inserts to reconstruct petgraph `NodeIndex` — the slot is never persisted. Lives in `anvil-graph-cache`, written/loaded by the daemon, **default-off** (`ANVIL_PERSIST_GRAPH`, fail-closed) + graduation criteria. Warm-start **restores indexes, never the verdict** (comes up `stale`; daemon MUST NOT certify until the content-hash-authoritative reconcile completes; no watermark, no staleness race). Corrupt/version/epoch/count/checksum mismatch ⇒ **cold rebuild, structured log, never panic/refuse-start**; durable atomic write (`O_EXCL`/`0600` temp, parent-dir fsync, EXDEV abort, `O_NOFOLLOW`, load size-cap); single private writer, lock-before-I/O, fallback never reads/writes snapshots. Schema-drift caught by a **golden round-trip fixture** (CI fails on any wire change ⇒ bump `schema_epoch`) + load-time **counts + CRC** (silent decode corruption can't survive to a verdict — content-hash reconcile alone wouldn't catch it); **discard-and-rebuild, never migrate**. **Privacy line:** sealed allowlist-only payload — structural identity only (cleartext names/specifiers/relative paths, hashes, span positions); **never** source/comments/docstrings/literal values or opaque `Vec<u8>`/`flatten`/`other`; paths MUST be workspace-relative (tested); residual secret-in-identifier risk accepted under same-uid/default-off/owner-only. Lifecycle GC of orphaned snapshots on start. Rejected: `rkyv`, `SQLite`, CBOR (graceful tolerance wrong for discard-on-doubt), bincode (v1/v2 wire-incompat), petgraph `serde-1` (reintroduces NodeIndex bug), event-sourced delta-replay + persist-and-trust `Clean` (per ADR-061), machine-derived structural fingerprint (not derivable in Rust without reflection) | Accepted |
| [070](070-daemon-windows-buildability.md) | Strategy for making the save-time daemon (`anvil-intercept`) Windows-buildable (it is pre-existing-red on `windows-msvc`). Survey finding: the blockers are a **small** set of unconditional imports from `#![cfg(unix)]` modules (`CanonicalChange` — a *neutral* enum living in unix-gated `change_class`; `path_safety::open_workspace_dirfd`), and the heavy subsystems (IPC named pipes, the notify-backed watcher, rayon pools, JobObject interrupt) are **already** cross-platform. So make the **verdict spine platform-neutral** over **three** swappable primitives, each with a real Windows impl: the read **anchor** (`WorkspaceAnchor` — Unix openat2 dirfd / the ADR-068 `read_safety` guard, built), the path **identity** (`PathIdentity` — Unix inode / Windows `FILE_ID_INFO` 128-bit file id, a real analogue not a stub), and **peer-auth** (owner-only pipe DACL + explicit `GetNamedPipeClientProcessId`→token-SID, the `SO_PEERCRED` parity). Two stages: **DSV-010a compile-clean** (decouple the neutral logic + anchor/identity abstractions wired Unix-only; daemon builds on Windows, verbs reply `not enabled`; unblocks the whole Windows rust matrix + lets the guard's tests run on CI), then **DSV-010b functional** Windows save-time (wire the Windows anchor/identity/peer-SID, enable verbs, extend the DSV-009 parity gate); DSV-011 clients follow. Rejected: separate Windows daemon (spine drift); permanently-disabled Windows verbs (adopted only as Stage 1); gate the whole crate `#![cfg(unix)]` (breaks the Windows `anvil` binary); a path+mtime identity stub (mis-classifies atomic-saves) | Accepted 2026-06-04 |
| [071](071-ast-aware-antipattern-detection.md) | Unblocks the AST-dependent half of RSTLAN-003 (cfg(test)-aware `unwrap`/`expect`/`panic`, SAFETY-comment-aware `unsafe`, serde hygiene) that the regex-only scanner can't express. The daemon links `anvil-checks` and ADR-064 forbids tree-sitter in the daemon; Cargo workspace feature unification means a feature flag wouldn't keep it out. Decision: a **new terminal crate `eddacraft-anvil-checks-ast`** (deps `anvil-checks` types + `tree-sitter-rust` workspace pin — not `anvil-kernel`) the daemon does **not** link, running **gate-time only** (`anvil check`/`gate`) per ADR-061's hot/non-hot split; save-time stays regex-only + parser-free. The dormant `Detection::Ast` variant becomes live (rule = tree-sitter query + Rust predicate table); guarded by a daemon-dep-boundary test + a registry-completeness test. Council reviewed (5 personas, SOUND-WITH-CHANGES — folded in). Rejected: feature flag on `anvil-checks` (unification breaks ADR-064), parse-then-feed to daemon (widens attestation), regex-only (FP bar), AST in `anvil-kernel` (layering) | Accepted 2026-06-04 (design council) |
| [072](072-git-native-governance-substrate.md) | Use Git as Anvil's durable governance substrate: durable evidence (witness, baseline, drift, exceptions, capsules, sealed Edda provenance, release attestations) is tracked, content-addressed, tamper-evident, and offline-verifiable using the repo itself — ratifying the posture already shipped for witness (ADR-037)/baseline (ADR-039)/drift (ADR-052). Git proves but does not serve hot reads (Kindling/Ember/GV2 stay local projections, ADR-035); Cloud is an optional amplifier never the source of truth (ADR-001); no secrets in durable evidence (generalises ADR-069 privacy line; council 2026-06-08: free-text fields are named scan-on-write surfaces, not aspiration); missing-evidence verdicts are `degraded` not `pass`. Umbrella for GITGOV | Accepted 2026-06-08 (full council) |
| [073](073-durable-vs-local-anvil-state.md) | Ratify the `anvil/` (tracked durable governance/memory/evidence) vs `.anvil/` (gitignored local runtime/cache/SQLite/logs) boundary already shipped for witness/baseline/drift, and record the two violations as required reconciliations: policy exceptions move from `.anvil/exceptions.json` → tracked `anvil/exceptions/` (EXCEPT — fixes a live bug where exceptions don't travel with the repo / aren't PR-reviewable; distinct from in-source `@anvil-ignore` ADR-004), and Edda memory moves `.anvil/edda/` → `anvil/edda/` (EDDA-SEAL — recorded debt until promoted to a module; secret-scan-on-migrate acceptance criterion). Read-fallback + no auto-delete migration discipline; boundary is enforcement-not-assertion (init seeds `.anvil/` wholesale + check-ignore sweep, GITGOV-014); dogfood-repo deviation (anvil/witness/ gitignored here) recorded | Accepted 2026-06-08 (full council) |
| [074](074-review-capsule-v0-format.md) | Review capsule v0 is a file-first, inspectable directory with a SHA-256-over-canonical-JSON digest manifest (`anvil.capsule.v1`); `witness.ndjson` embeds **verbatim** `anvil-witness::WitnessLine` records so verification reuses `verify_chain_dag` (not a parallel parser — the brainstorm's `L0..L4` `WitnessExtract` is fiction), `rules_sha` via `anvil_rules::rules_sha`, SARIF via the ADR-058 emitter (no unified finding model). Closed-state verdicts (`pass`/`warn`/`degraded`/`block`/`error`, missing≠pass) are advisory evidence not a new block (ADR-002/-042). Git bundles, refs/notes, signing, detached-verification semantics deferred past v0 | Accepted 2026-06-08 (full council) |
| [080](080-ungate-welcome-demo-surface.md) | Remove `welcome` from `CLI_GATED_COMMANDS`: the discovery golden path runs unauthenticated (discovery scan, tutorial, hub) as the beta demo surface, mirroring the already-ungated `dashboard`; the welcome ending points at the still-gated `anvil start`, so the licence wall sits where ongoing value begins. Durable surfaces (`init`, `start`, `watch`, `check`, `status`, `gate`, …) stay gated. Accepted consequence: welcome's guided setup can seed `.anvilrc` without auth (config seeding is not gated value). Closes UJ-004; gate posture still revisited at GA | Accepted 2026-06-10 (operator) |
| [079](079-watch-daemon-guidance-only.md) | `anvil watch` stays **guidance-only** when no daemon answers: warn once per disconnect, name `anvil start` as the recovery step, scoped-check fallback — no interactive offer-to-start prompt, no auto-start. Amends the ADR-075 rollout posture; closes UJ-007 with zero code (the guidance surface shipped via UJ-001/-005/-006: every onboarding ending, `watch --help`, the fallback advisory, and the `anvil status` posture line all name `anvil start`). Revisit at GA if users persistently stay on the fallback path | Superseded 2026-06-15 by ADR-082 |
| [078](078-capsule-retention-and-prune.md) | Capsule retention is **keep-until-explicitly-pruned** — nothing in Anvil ever deletes capsule evidence automatically (no create-time rotation, no hooks, no age GC; ADR-074 sub-decision). External `--out` stays the default; in-repo staging (`anvil/evidence/capsules/`, ADR-073) is opt-in and accumulates indefinitely **as a stated choice**. `anvil capsule prune` is the explicit disposal surface: schema-gated candidates (only parseable `anvil.capsule.v1` dirs; symlinks skipped), `--keep-last N` (N≥1, 0 refused) ordered by (head committer date, head SHA, dir name) — frozen manifest has no timestamp; mtimes don't survive clones — unordered capsules always kept outside the N accounting, **dry-run by default**, `--apply` re-scans + deletes **via the git index** (fs-only delete is silently reversible by `git restore`), never commits; exit 0 success / 1 runtime error (usage errors, incl. `--keep-last 0`, exit 2 per the CLI-wide clap convention), warnings on stderr; `--json` deferred | Accepted 2026-06-10 (council) |
| [076](076-feature-catalogue-surface-registry.md) | Make the **surface/feature the catalogue's primary noun** and register the full ~38-command inventory (~43 features / 9 categories) as multi-axis features, extending ADR-048 from "how to structure a new gate" to "what the product is made of". A **feature** is the smallest unit you'd ship/gate independently; **categories** group them (a `foundational` category holds base plumbing). **No new class/dimension**: the four reasons to flag map onto existing classes + axes — tiered benefit & auth → `Entitlement` (+ audience), safety → `OpsKillSwitch`, staged availability → the environment axis; **edition ≡ entitlement**, **auth ≡ entitlement + auth-bearing audience**. Stays lean via **category defaults + per-feature overrides**; auth **posture** becomes **catalogue-declared** — hosts derive their own enforcement (closes the unauthenticated-by-absence gap that produced CIB-046). Editions resolve **at runtime by config** (one binary); build-time cargo-feature omission **deferred**. **Open questions resolved 2026-06-09:** no enforcement-point field (presence gates the surface on/off; behaviour is the orthogonal value-when-on); explicit **`requires` dependency edges** (declared + static blast-radius/acyclicity; **runtime cascade-off deferred**); storage = dedicated **`flags/surfaces.json`** registry (manifest stays the policy layer); dashboard split **per-view** (each `requires` its engine) + `dashboard.saved`; **`admin.credential`** split to `foundational`; staff-axis resolution **deferred to RBAC**. **Council pass (5 reviewers) folded in 2026-06-09**: posture-not-lists (host keeps enforcement ownership + `MUST_ALWAYS_BE_OPEN` floor), dependency narrowed to declared+static, `off ⇒ refuse` exceptions for system-invoked/recovery-critical surfaces, env-axis runtime gap + AND-precedence flagged, honest schema-fit re-label. | Proposed |
| [077](077-cert-closure-depth-cap.md) | Bound the save-time **certifiability reverse-impact closure** by the ADR-063 hard depth cap (`MAX_REVERSE_IMPACT_DEPTH = 2`), not only the file-count budget — refining ADR-061 §6's "re-exports recurse, bounded by budget". `crate::certify::certify`'s `impact_closure` (today an unbounded transitive walk) becomes depth-capped, matching `HotReadApi::reverse_impact`; this takes **path A** of the `HotReadApi::certify` hand-off so GV2-024 seals `HotReadApi` uniformly (no unbounded traversal reachable on the hot type). **Coverage-verdict- and soundness-preserving**: the closure only *sizes* the `Partial` `StaleReason` (the set is discarded) and is never inline-validated, so capping only relabels the wire `StaleReason` `impact-set-overflow` → `cross-file-resolution-needed` for >2-hop over-budget chains (monotone; no new enum value; both route to the same background scan; only display/telemetry renderers consume it). `backing_parity` re-baselined under the cap (warm==cold by construction). Configurable 1→2-hop lever stays GV2-026. Resolves the GV2-027 deferred owner decision | Accepted 2026-06-09 (owner, PR #2458) |
| [098](098-policy-enforcement-reset-gate.md) | **POLRESET-001 policy-value/enforcement design gate** reconciling ADR-002/015/037/040 (operator-ratified, all gate questions). **AD-1** full OPA→regorus replacement (no frozen reference crate) as replace-then-delete: PR-A deletes `opa_capabilities.rs`/`opa_real_binary.rs` + fixes POLRESET-007 `policies/eval/`→`policies/fixtures/`; PR-B (=OPAE-003) repoints `run_check_policy` (`gate.rs:1736`)+`policy list/explain`+`builtin_policies()` onto the `anvil-policy-engine` facade, parity-gated; PR-C deletes `opa.rs`/`evaluator.rs`/`loader.rs`/`bundle.rs`/`library.rs` + moves `Violation` into `exceptions.rs`. `opa test policies/fixtures/`+regal+poleng-parity CI stays (ADR-040 D-1/D-5); eval-regression impact zero; regorus port closes the dormant silent `passed:true` gap. **AD-2** topology: `anvil-policy-engine`=product path; new graph-free `anvil-exceptions` (trigger `min(EXCEPT-006, anvil-policy disposition)`); harness+`list/explain` fold into `anvil-cli` (repoint onto `anvil-checks` AP registry, retire the 22-entry mirror); `anvil-policy` ultimately deleted. **AD-3** two-axis vocabulary NOW: outcome=`ControlDecision` gains `Fence`+`#[serde(other)] Unknown` (last breaking extension); daemon `Mode`+MCP `EnforcementMode` collapse into one kernel-types posture type+alias table; lossy `fence→Block` moves to action-time projection; stricter-wins `off<warn<fence<interrupt`; `Unknown→Warn`+telemetry; 3 same-change amendments (`is_veto()` over `decision=="block"` string compare, audit every `matches!()`, per-occurrence divergence telemetry). **AD-4** pre-write=existing surfaces only (MCP `validate_write`+`gate`+CI, no new interception layer); ADR-061 §6 upheld (no policy family on `validate_paths`); `daemon_dep_boundary.rs` extended to forbid `regorus`/`anvil-policy*`; ADR-067 injected-trait reserved as the only future daemon on-ramp. **AD-5** rule trait stays binary Allow\|Interrupt (routing at adapter); `warn` default (ADR-002); mandatory out-of-band `ANVIL_POLICY_ENFORCEMENT` kill switch bypassing `.anvil.yaml`; fail-open eval budget; warn-mode exception labelling. **AD-6** EXCEPT-006 verdict-aware wiring HARD-gates fence/interrupt; exceptions store stays in `anvil-policy` slice 1. **AD-7** amends nothing in 002/037/040; ratifies ADR-015; names acceptance-policy vs code-policy; `validate_at_l4`-over-regorus=future intake; downstream unblock deps-only (POLRESET-002/003/004/010→Ready). Council plan-18c47503 (architect, pragmatic-lead, adversarial-reviewer) | Accepted 2026-07-04 (operator, planning council plan-18c47503) |

## Planning and Process

| ADR | Decision | Status |
|-----|----------|--------|
| [034](034-cross-cutting-modules-as-aps-primitive.md) | Promote cross-cutting modules to a first-class APS primitive in `aps-rules.md`; LAUNCH (first trial) and TRACE (second trial) cite the spec by anchor; `Blocks on:` callout type carried as provisional until exercised through a real close | Accepted |
| [042](042-closeout-enforcement-exit-codes.md) | Closeout-enforcement checks (`adr:check`, `aps:drift`, future `docs:check`) are a named carve-out from ADR-002 — they exit non-zero on violation by design. ADR-002 continues to govern runtime warnings on user code. | Proposed |
| [053](053-advisory-aps-index-counts.md) | Per-module APS index `N/M` counts are advisory-derived from per-item `Status:` lines, not PR-maintained; feature PRs never edit the count, a single-writer periodic reconcile refreshes it, and `aps:index:check` freshness is advisory (scoped exception to ADR-042). Post-merge regen bot recorded as the escalation. | Accepted |
| [119](119-documentation-freshness-from-declared-upstream.md) | Documentation freshness is decided from the `Upstream` paths each governed doc already declares versus git history — not a calendar (ADR-117: a gate fails only for what a commit did). New `docs-owed` surface. **File-level** upstreams gate on the PR that moves them; **directory/glob** upstreams are advisory (measured: `crates/anvil-cli` alone triggers 7 findings; 50 of 86 owed findings are file-level-only, 20 directory-only, 16 mixed). Only the `owed` class gates (upstream moved, doc not committed since — 86) versus advisory `review` (7); same-day counts as reviewed, so the check under-reports by design. **Public docs get a different trigger**: they describe the shipped release, not `main`, so governance moves into non-rendered YAML frontmatter (`owner`, `upstream`, `verified_against` — precedent: `public_unlisted` at `check-public-docs.mjs:182`; the visible table stays removed) and verification runs at the **release boundary** via `git diff v<verified_against> <candidate> -- <upstream>` — anchored on each page's OWN last-verified version, not the release's `previous_tag`, which would report a page clean whenever its upstream moved in a release it skipped; missing or unknown `verified_against` is an error, never a pass. Generalises `public-reference-regen.yml` (#3676) from one generator to the whole public surface. Executable verification outranks declared: `ANVIL_DOCS_VERSION` must track the current release (pinned `0.9.1-beta` against a `0.9.4-beta` repo today). Baselined ratchet under the ADR-042 carve-out; coverage counts always reported (only 103 of 228 docs are checkable). | Proposed |
| [075](075-v080-graph-product-scope.md) | Back `v0.8.0-beta` with the Graph V2 **A′ slice** (GV2-027 critical-path foundation + the A→A′ backing swap) and flip `ANVIL_WATCH_DAEMON` default-on (with rollout controls) once the §8 bar + A′ swap are green. Defer **GCTX + non-critical-path GV2** (013/014/020/023/026) and persistence (030/Sub-phase B) to v0.9. Reverses the RELEASE-PLAN deferral; builds on (not supersedes) ADR-061/063/064. | Accepted |

## Agent Infrastructure

| ADR | Decision | Status |
|-----|----------|--------|
| [024](024-internal-agent-harness.md) | Thin agent runtime (weave, Apache-2.0) standalone at eddacraft/weave-rs; anvil-weave harness with zero-copy graph access | Proposed |

## Evaluation and Testing

| ADR | Decision | Status |
|-----|----------|--------|
| [013](013-eval-harness-adoption.md) | External eval harness behind `EvalHarnessPort` adapter boundary | Proposed |

## Edge and Infrastructure

| ADR | Decision | Status |
|-----|----------|--------|
| [032](032-edge-architecture-afd.md) | Azure Front Door as the canonical edge layer for eddacraft.ai; supersedes the implicit "Vercel as both origin and edge" assumption | Accepted |

## Language and Coverage

Decisions supporting the [2026-04-08 Language and Coverage Design](../specs/2026-04-08-language-and-coverage-design.md).

| ADR | Decision | Status |
|-----|----------|--------|
| [065](065-rust-t3-architecture-enforcement.md) | Rust T3 architecture enforcement location is Rust-native (`crates/anvil-architecture` + kernel parser edges); no TS shim. Authoritative for layer/boundary, baseline, `architecture-validate` on Rust (and future anchors). TS analyser is legacy surface only. Realises council §16.5 #5 (C-019); unblocks RSTLAN Ready promotion and NBI re-eval completion. | Accepted |
| [027](027-pack-architecture.md) | Per-pack crate, kernel symbol-graph access, compiled-in activation; `crates/anvil-pack-{name}/` registered through `crates/anvil-packs/` | Accepted |
| [028](028-markdown-governance-crate.md) | Markdown governance lives in standalone Rust crate `crates/anvil-markdown-governance/` with `pulldown-cmark` — not the kernel | Accepted (rationale strengthened by ADR-033) |
| [029](029-suppression-parser-authority.md) | Rust suppression parser is authoritative for new surfaces; no new comment styles added to the TS parser | Accepted (amended by ADR-033 — TS parser retired) |
| [030](030-surface-drivers-supersede-napi-cutover.md) | Surface drivers (editor + MCP) on the intercept daemon supersede TSRET-003/-004; TSRET-005 retargeted; napi publication no longer required | Proposed (sequencing amended by ADR-033) |
| [087](087-security-antipattern-category.md) | New `insecure-construction` `AntiPatternCategory` (named to avoid the SEC CI-pipeline module collision), populated **only** by the syntactic-smell subset of [sec-context](https://github.com/Arcanum-Sec/sec-context): first-wave families `weak-cryptography` (MD5/SHA1/DES, ECB, JWT `none`) + `unsafe-rendering` (DOM-XSS sinks), regex/enabled; deferred opt-in `injection-smell` (SQLi/command/LDAP/XPath/NoSQL, AST gate-time — smell not taint) + insecure-RNG; SSTI extends `dynamic-execution`; credentials stay with the `secret` check. Reuses the ADR-071 regex+AST scanners — **no new engine check, no taint analysis**. Taint-class (reflected/stored XSS, output encoding) and absence-class (rate limiting, MFA, CSP, server-side validation) items are **out of model** by explicit scope boundary; crossing it needs a new ADR. Preserves warnings-over-blocks. First-wave realised by the INSEC catalogue module. | Accepted 2026-06-18 (operator) |
| [110](110-fragile-presentation-antipattern-family.md) | Presentation-correctness construction smells enter the anti-pattern scanner under the ADR-087 bar: single-file, single-line deterministic detection of a construction whose failure mode is a **correctness loss**, human confirms the contract via the standard suppression flow. New `fragile-presentation` family + first-class category ships FRAG-001 — the invisible-content trap (`initial={{ opacity: 0, ... }}` entrance-animation gating; a reduced-motion preference, backgrounded tab, or hydration miss leaves the section permanently blank) — warning/medium/default-on, JS/TS extensions only (CIB-198). **Design-taste rules rejected** for the catalogue (font blacklists, palette/layout fashion — non-deterministic, fashion-bound); **render-time checks stay out of model** (contrast, clipping, alignment, dead controls, image seams need a rendered DOM — a future render-time check category is its own ADR, justified against axe-core/Lighthouse overlap first). Known regex gaps (multi-line `initial` objects, `variants`, class-toggle reveals) deferred to a JS/TS AST path per ADR-071, not stretched regexes | Accepted 2026-07-16 (operator) |
| [088](088-dpo-observation-kind-taxonomy.md) | Observation-kind taxonomy for the [DPO](../modules/daemon-protection-observability.aps.md) module (producer coverage for daemon protection; today only mid-edit emits to Kindling, the save-time `validate_paths` path + `fence.rs` emit nothing). **Decision 1:** save-time verdicts use canonical Anvil-owned payload kind `gate_evaluated` with a pinned `SAVE_TIME_GATE_ID` (a verdict *is* a gate evaluation); ADR-116 limits legacy `gate.evaluated` acceptance to migration/dual-read. **Decision 2:** fence/cascade events use the distinct Anvil-owned payload kind `constraint_applied` via a new defaulted `KindlingObservationSink` method (mirroring `try_emit_action_executed`) — **not** `gate_evaluated/Fail`, which would count fence lockouts as rule violations and write internally-inconsistent rows (empty `rules_evaluated` + `outcome:Fail`). Both kinds live inside `anvil.governance.v1` over upstream `generic-event@1`; ADR-116 assigns mapping to KFIT-007 and supersedes the archived KDS/upstream Anvil-kind mechanism. **Decision 3:** ADR is the human registry for the kind/`gate_id` namespace (no compile-time dup guard). **Decision 4:** config-gated path inclusion; fence `reason` always normalised. Non-blocking emission, fence ordering, legacy NDJSON migration, and the rate cap are implementation decisions in the DPO/KFIT modules, not this ADR. Synthesised by planning council `plan-a50aa93d` (architect + adversarial). | Accepted 2026-06-20; wire spelling and mechanism amended 2026-08-03 by ADR-116 |
| [089](089-fp-telemetry-destination.md) | False-positive reporting (OPSUP-007) telemetry **destination = the local Kindling record**; no telemetry leaves the machine as part of OPSUP-007. `anvil report-fp` writes the Anvil-owned `false_positive_reported` payload kind inside `anvil.governance.v1`, carried by upstream `generic-event@1` (stable `ANV-*` check ID, **hashed** path via `sha256_hex`, rule context, timestamp; source snippets opt-in only, never default — fail-closed); the old “13th Kindling kind” ordinal is legacy only. Unknown check IDs are rejected against the OPSUP-001 registry. **Zero network call** — stays inside the air-gap guarantee (`docs/runbooks/anvil-air-gapped.md`). **Egress is explicitly deferred and de-scoped**: any off-machine transmission is a separate future opt-in work item with its own ADR (the air-gap boundary), **not** a new always-on endpoint. Chosen over an opt-in `export` bridge (larger scope, forces an egress contract now) and a new endpoint (breaks air-gap, needs a superseding ADR). ADR-116 amends immutable wording to append-only normal operation with one authenticated, explicit, receipted governance-prune exception. | Accepted 2026-06-21; wire model and retention wording amended 2026-08-03 by ADR-116 |
| [093](093-tail-wave-2-wasm-text-and-zig-reentry.md) | Owner-directed addition of two languages to the Track 2 tail at **T1**, batched as `lang-tail-wave-2` (LTW2): **WebAssembly text** (`.wat`/`.wast`) — never a prior candidate — and **Zig** (`.zig`/`.zon`) — **re-entry** from the §13 cut list. Honest §6 re-score: confirmed demand 0 for both; proceeds on owner override (not invented user demand), recorded so it is not a silent re-add. **Scope is text-only — binary `.wasm` is excluded** (binary ≠ source). **T1 only** — no T2 catalogue (Zig's archived 6-pattern set does **not** re-enter). Grammar maturity audit (LTW2-001) is the gate: each candidate must bind tree-sitter 0.26 or be dropped (C-005 precedent); ABI compat unverified, so the wiring items stay Proposed until it passes. Supersedes the §13 cut for Zig. | Accepted 2026-06-29 (owner) |
| [118](118-csharp-anchor-promotion-t2-t3.md) | Owner-directed **promotion of C# / .NET out of the Track 2 tail (T1) into Track 1 anchor work targeting T3**, as the new active module `lang-dotnet-anchor` (DNLAN). Honest §6 re-score: confirmed demand **0** — the §8.2 "first .NET user" promotion lever has **not** fired; proceeds on owner override, recorded so the promotion is not silent (ADR-093 precedent). Supersedes the promotion lever for C# only; it stays in force for Dart, Go, Java, Kotlin, C/C++. Acceptance is the LANGTS T3 checklist plus the §16.5 #9 FP bar on public OSS (Anvil has no C#) — **no partial anchors** per §8.1, so a shipped catalogue without boundary enforcement is C#-at-T2, not .NET support. Scope stays **C#-only** (`.vb`/`.fs` do not enter). The archived `lang-dotnet` module is **not** un-archived; it stays the historical record with a forward pointer. | Accepted 2026-08-06 (owner) |

## Superseded

| ADR | Replaced By | Reason |
|-----|-------------|--------|
| [008](008-ink-vs-ratatui-assessment.md) | ADR-011a, Rust kernel | TUI choice tied to language; Rust migration changed the calculus |
| [009](009-ink-vs-ratatui-watch-mode-performance.md) | ADR-011a, Rust kernel | Confirmed rendering isn't the bottleneck; check execution is |
| [010](010-pulumi-typescript-iac.md) | ADR-007 | Duplicate; ADR-007 is the canonical Pulumi decision |
| [011a](011a-rust-core-engine.md) | Architecture evolution docs | Rust kernel spec and architecture evolution are now authoritative |

### Module-level supersession (in-flight validation thesis)

The two real-time-validation modules predate the drivers-on-daemon architecture
and are superseded — recorded here (not as ADRs) per RTAI-009:

- **RTVS** (`real-time-validation-simplified`) — archived 2026-04-24; written
  against the retired Ink/TS stack.
- **RTVF** (`real-time-validation-full`) — superseded 2026-04-24; its "unified
  validation server" framing predates ADR-030.

Both are superseded by **RTAI**
([`realtime-ai-validation`](../modules/realtime-ai-validation.aps.md)), which is
the realisation of the in-flight validation thesis on the drivers → daemon
architecture (ADR-030): the intercept daemon's `scan_buffer` RPC (RTAI-002)
validates an unsaved buffer against the same INTR rule registry as the
save-time path, the MCP pre-write surface (RTAI-006) ships that semantics to
agents, and every mid-edit decision mirrors onto the INTD-013 notification lane
with a `mirror.path = "midEdit"` discriminator (RTAI-007). There is no separate
validation server.
