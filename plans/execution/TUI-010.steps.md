# Steps: TUI-010 — `anvil gate` interactive explorer

| Field      | Value                                          |
| ---------- | ---------------------------------------------- |
| Source     | [../modules/tui.aps.md](../modules/tui.aps.md) |
| Task(s)    | TUI-010 — `anvil gate` interactive explorer    |
| Created by | AI                                             |
| Status     | Planned                                        |

## Prerequisites

- [ ] TUI-001 complete (component library available)
- [ ] Existing `anvil gate` command functional

## Context

Enhance `anvil gate` output with an interactive explorer for drilling into
failures. Features: collapsible check tree, detail panel, failure navigation
(n/p for next/previous), filter controls.

## Steps

### 1. Define explorer state model

- **Checkpoint:** GateExplorerState with checks, selectedCheck, filters
- **Checkpoint:** Check tree structure supports expand/collapse
- **Files:** `cli/src/tui/commands/gate/types.ts`

### 2. Create GateExplorer container

- **Checkpoint:** Two-panel layout: CheckTree (left), DetailPanel (right)
- **Checkpoint:** Filter bar at top
- **Checkpoint:** Keyboard navigation (j/k, enter, n/p, q)
- **Files:** `cli/src/tui/commands/gate/GateExplorer.tsx`

### 3. Implement CheckTree panel

- **Checkpoint:** Hierarchical tree of checks (grouped by category)
- **Checkpoint:** Expand/collapse with arrow keys or enter
- **Checkpoint:** Visual indicators: ✓ pass, ✗ fail, ○ skipped
- **Checkpoint:** Highlight currently selected check
- **Files:** `cli/src/tui/commands/gate/panels/CheckTree.tsx`

### 4. Implement DetailPanel

- **Checkpoint:** Shows full details of selected check
- **Checkpoint:** For failures: error message, file location, suggestion
- **Checkpoint:** Scrollable if content exceeds panel height
- **Files:** `cli/src/tui/commands/gate/panels/DetailPanel.tsx`

### 5. Implement FilterBar

- **Checkpoint:** Filter by status: all, passed, failed, skipped
- **Checkpoint:** Filter by category if applicable
- **Checkpoint:** Search by check name
- **Files:** `cli/src/tui/commands/gate/panels/FilterBar.tsx`

### 6. Implement failure navigation

- **Checkpoint:** `n` key jumps to next failure
- **Checkpoint:** `p` key jumps to previous failure
- **Checkpoint:** Shows "X of Y failures" indicator
- **Files:** `cli/src/tui/commands/gate/GateExplorer.tsx`

### 7. Integrate with gate command

- **Checkpoint:** `anvil gate <plan> --tui` launches explorer
- **Checkpoint:** Falls back to standard output when TUI unavailable
- **Checkpoint:** Explorer shows after gate execution completes
- **Files:** `cli/src/commands/gate.ts`

### 8. Add tests

- **Checkpoint:** CheckTree navigation tests
- **Checkpoint:** Filter functionality tests
- **Checkpoint:** Failure navigation tests
- **Validate:** `nx test cli --testNamePattern="gate explorer"` — passes
- **Files:** `cli/src/tui/commands/gate/**/*.test.tsx`

## Completion

- [ ] All checkpoints validated
- [ ] Keyboard navigation intuitive and documented
- [ ] Task marked complete in source module
