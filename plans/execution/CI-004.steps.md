# Steps: CI-004

| Field      | Value                                                                |
| ---------- | -------------------------------------------------------------------- |
| Source     | [../modules/ci-integration.aps.md](../modules/ci-integration.aps.md) |
| Task(s)    | CI-004 — Configuration and documentation                             |
| Created by | AI                                                                   |
| Status     | Pending                                                              |

## Prerequisites

- [ ] CI-003 complete (PR comment and status check)

## Context

Provide comprehensive documentation and configuration options for users to adopt
the GitHub Action in their repositories.

## Steps

### 1. Write action README

**File:** `.github/actions/anvil-check/README.md`

Include:

- Quick start with copy-paste workflow
- All inputs with descriptions and defaults
- All outputs with usage examples
- Permissions required
- Monorepo support (working-directory)
- Troubleshooting common issues

### 2. Create standard workflow example

**File:** `.github/workflows/anvil.yml.example`

```yaml
name: Anvil Check

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  statuses: write
  checks: write

jobs:
  anvil-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Anvil Check
        uses: ./.github/actions/anvil-check
        with:
          node-version: '20'
          fail-on-warnings: false
```

### 3. Create monorepo matrix example

```yaml
jobs:
  anvil-check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package: [core, cli, adapters]
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/anvil-check
        with:
          working-directory: packages/${{ matrix.package }}
```

### 4. Update project documentation

**Files to update:**

- `docs/QUICK_START.md` — Add CI setup section
- `docs/USER_GUIDE.md` — Add GitHub Action chapter
- `README.md` — Add CI mention and badge placeholder

### 5. Add .anvilrc CI configuration support

Document CI-specific config in `.anvilrc`:

```json
{
  "ci": {
    "failOnWarnings": false,
    "postComment": true,
    "annotationsLimit": 50
  }
}
```

### 6. Create CHANGELOG entry

```markdown
## [Unreleased]

### Added

- GitHub Action for CI/CD integration (`anvil-check`)
  - Automatic changed-files detection
  - PR comment summaries
  - Commit status checks (informational by default)
  - Inline annotations in PR files view
  - Optional blocking mode via `fail-on-warnings`
```

## Acceptance Criteria

- [ ] README fully documents all options
- [ ] Copy-paste workflow example works out of the box
- [ ] Monorepo example provided
- [ ] Main docs updated with CI section
- [ ] .anvilrc CI configuration documented
