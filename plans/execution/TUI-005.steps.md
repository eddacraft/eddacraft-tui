# Steps: TUI-005 — First-run welcome experience

| Field      | Value                                          |
| ---------- | ---------------------------------------------- |
| Source     | [../modules/tui.aps.md](../modules/tui.aps.md) |
| Task(s)    | TUI-005 — First-run welcome experience         |
| Created by | AI                                             |
| Status     | Complete                                       |

## Prerequisites

- [x] TUI-001 complete (component library available)

## Context

Detect first-time Anvil usage and display a friendly welcome screen explaining
the value proposition. Goal: make Anvil's value clear within 30 seconds.

First-run detection via `.anvil/first-run` marker file.

## Steps

### 1. Implement first-run detection

- **Checkpoint:** `isFirstRun()` checks for `.anvil/first-run` marker
- **Checkpoint:** `markFirstRunComplete()` creates marker file
- **Checkpoint:** Respects `ANVIL_SKIP_WELCOME=1` environment variable
- **Files:** `cli/src/services/first-run-detector.ts`

### 2. Create Welcome TUI component

- **Checkpoint:** Shows Anvil logo/banner
- **Checkpoint:** Brief value proposition (2-3 sentences)
- **Checkpoint:** Quick start options: Init, Tutorial, Skip
- **Files:** `cli/src/tui/commands/welcome/Welcome.tsx`

### 3. Define welcome content

- **Checkpoint:** Value proposition copy is clear and compelling
- **Checkpoint:** UK English spelling throughout
- **Checkpoint:** No marketing fluff — focus on developer benefit
- **Files:** `cli/src/tui/commands/welcome/content.ts`

### 4. Integrate with CLI entry point

- **Checkpoint:** Welcome shown on first bare `anvil` command
- **Checkpoint:** Welcome NOT shown for explicit commands (`anvil init`, etc.)
- **Checkpoint:** Welcome shown only once (marker file created after)
- **Files:** `cli/src/index.ts`

### 5. Add tests

- **Checkpoint:** First-run detection tests
- **Checkpoint:** Welcome component render tests
- **Validate:** `nx test cli --testNamePattern="welcome\|first-run"` — passes
- **Files:** `cli/src/services/__tests__/first-run-detector.test.ts`

## Completion

- [x] All checkpoints validated
- [x] Welcome explains value in < 30 seconds reading time
- [x] Task marked complete in source module

## Implementation Notes

**Completed:** 2025-12-30

**Files Created:**

- `cli/src/services/first-run-detector.ts` — First-run detection service
- `cli/src/services/__tests__/first-run-detector.test.ts` — 15 tests
- `cli/src/tui/commands/welcome/Welcome.tsx` — Welcome TUI component
- `cli/src/tui/commands/welcome/content.ts` — Value proposition copy
- `cli/src/tui/commands/welcome/index.ts` — Export barrel
- `cli/src/tui/commands/welcome/__tests__/welcome.test.tsx` — 19 tests
- `cli/src/commands/welcome.ts` — Welcome command handler

**Features:**

- First-run detection via `.anvil/` marker directory
- `ANVIL_SKIP_WELCOME=1` environment variable to disable
- ASCII art logo banner
- Keyboard-navigable menu (Init, Tutorial, Skip)
- Integrated with CLI entry point (`cli/src/index.ts`)
- Only shows on bare `anvil` command, not explicit commands

**Bug Fixes Applied:**

- Welcome TUI exits properly after selection (calls `exit()`)
- No setInterval leak (uses `waitUntilExit()` instead of polling)
- ESM-compatible imports (no `require('fs')`)
- ASCII-only icons for terminal compatibility

**Test Results:** 34 tests passing (15 first-run + 19 welcome)
