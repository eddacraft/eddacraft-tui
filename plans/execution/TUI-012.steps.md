# Steps: TUI-012 — Log panel with filtering

| Field      | Value                                          |
| ---------- | ---------------------------------------------- |
| Source     | [../modules/tui.aps.md](../modules/tui.aps.md) |
| Task(s)    | TUI-012 — Log panel with filtering             |
| Created by | AI                                             |
| Status     | Complete                                       |

## Prerequisites

- [ ] TUI-001 complete (component library available)

## Context

Reusable scrollable log panel with level filtering (error/warn/info) and search.
Used in watch dashboard and gate explorer for verbose output. Supports
copy-to-clipboard for sharing.

## Steps

### 1. Define log entry model

- **Checkpoint:** LogEntry type with timestamp, level, message, source
- **Checkpoint:** LogLevel enum: error, warn, info, debug
- **Files:** `cli/src/tui/components/display/types.ts`

### 2. Implement LogPanel component

- **Checkpoint:** Scrollable container with keyboard navigation (j/k, g/G)
- **Checkpoint:** Auto-scroll to bottom on new entries (toggleable)
- **Checkpoint:** Shows timestamp and level prefix for each entry
- **Files:** `cli/src/tui/components/display/LogPanel.tsx`

### 3. Implement LogFilter component

- **Checkpoint:** Toggle buttons for each log level
- **Checkpoint:** Active filters highlighted
- **Checkpoint:** Filter state persisted during session
- **Files:** `cli/src/tui/components/display/LogFilter.tsx`

### 4. Implement LogSearch component

- **Checkpoint:** Search input filters visible entries
- **Checkpoint:** Highlights matching text in entries
- **Checkpoint:** Shows "X of Y matches" count
- **Checkpoint:** n/N to navigate between matches
- **Files:** `cli/src/tui/components/display/LogSearch.tsx`

### 5. Add copy-to-clipboard support

- **Checkpoint:** `y` key copies selected entry to clipboard
- **Checkpoint:** `Y` key copies all visible entries
- **Checkpoint:** Visual feedback on copy (brief flash/message)
- **Files:** `cli/src/tui/components/display/LogPanel.tsx`

### 6. Integrate with watch and gate commands

- **Checkpoint:** Watch dashboard includes log panel option
- **Checkpoint:** Gate explorer includes log panel for verbose output
- **Checkpoint:** `--verbose` flag enables detailed logging
- **Files:** `cli/src/tui/commands/watch/WatchDashboard.tsx`,
  `cli/src/tui/commands/gate/GateExplorer.tsx`

### 7. Add tests

- **Checkpoint:** LogPanel scrolling tests
- **Checkpoint:** Filter functionality tests
- **Checkpoint:** Search functionality tests
- **Validate:** `nx test cli --testNamePattern="log panel"` — passes
- **Files:** `cli/src/tui/components/display/**/*.test.tsx`

## Completion

- [x] All checkpoints validated
- [x] Log panel handles 1000+ entries without lag
- [x] Task marked complete in source module
