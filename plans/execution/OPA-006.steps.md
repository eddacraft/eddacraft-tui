# Steps: OPA-006

| Field   | Value                                                                                        |
| ------- | -------------------------------------------------------------------------------------------- |
| Source  | [../modules/opa-architecture-integration.aps.md](../modules/opa-architecture-integration.aps.md) |
| Task    | OPA-006 — OPA input schema enhancement                                                       |
| Status  | In Progress                                                                                  |

## Prerequisites

- [x] OPA-005 complete (ArchitectureCheck produces architectureContext)
- [x] OPA-007 complete (gate runner passes context between checks)
- [x] OPAInput interface has architecture field

## Context

The gate runner already:
1. Runs ArchitectureCheck first
2. Extracts `architectureContext` from result details
3. Passes it to subsequent checks via `CheckContext.architectureContext`

PolicyCheck has `OPAInput.architecture` field but doesn't populate it from context.

## Steps

### 1. Update PolicyCheck to consume architectureContext

- **Checkpoint:** PolicyCheck reads `context.architectureContext` in `run()` method
- **Validate:** `nx test core --testNamePattern="policy.check"`

### 2. Map ArchitectureContext to OPAInput.architecture

- **Checkpoint:** `buildOPAInput()` populates architecture field from context
- **Pattern:** `core/src/gate/checks/policy.check.ts:buildOPAInput`

### 3. Include layer definitions in architecture input

- **Checkpoint:** OPA input includes layers from architecture definition
- **Validate:** `nx test core --testNamePattern="policy.check"`

### 4. Include violation summary for policy decisions

- **Checkpoint:** OPA input includes violation counts by type
- **Validate:** Rego policy can query `input.architecture.violations`

## Completion

- [ ] All checkpoints validated
- [ ] Task marked complete in source module
