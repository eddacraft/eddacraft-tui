<!-- APS Execution: See docs/ai/prompting/steps.prompt.md -->

# Steps: CORE-001-003

| Field      | Value                                                                                                |
| ---------- | ---------------------------------------------------------------------------------------------------- |
| Source     | [../modules/save-time-trust.aps.md](../modules/save-time-trust.aps.md)                               |
| Task(s)    | CORE-001 — Warning schema definition; CORE-002 — Check runner refactor; CORE-003 — CLI check command |
| Created by | @codex / AI                                                                                          |
| Status     | Completed                                                                                            |

## Prerequisites

- [x] Existing gate runner and CLI command structure available

## Steps

### 1. Define warning schema types and helpers

- **Checkpoint:** `core/src/antipattern/types.ts` defines `WarningSchema`,
  `WarningResultSchema`, and helper utilities (e.g. `createWarningResult`).
- **Pattern:** `core/src/antipattern/types.ts`

### 2. Add planless analysis entry point in GateRunner

- **Checkpoint:** `GateRunner.analyzeFiles()` aggregates warnings and returns
  `AnalyzeResult` with tests in `core/src/gate/gate-runner.test.ts`.
- **Pattern:** `core/src/gate/gate-runner.ts`

### 3. Add CLI command for planless checks

- **Checkpoint:** `cli/src/commands/check.ts` runs `anvil check <files...>` with
  JSON and human output plus tests in `cli/src/commands/check.test.ts`.
- **Pattern:** `cli/src/commands/check.ts`

## Completion

- [x] All checkpoints validated
- [x] Task(s) marked complete in source module

**Completed by:** @codex / AI
