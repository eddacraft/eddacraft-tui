# Steps: TUI-011 — Parallel progress visualisation

| Field      | Value                                          |
| ---------- | ---------------------------------------------- |
| Source     | [../modules/tui.aps.md](../modules/tui.aps.md) |
| Task(s)    | TUI-011 — Parallel progress visualisation      |
| Created by | AI                                             |
| Status     | Complete                                       |

## Prerequisites

- [ ] TUI-001 complete (component library available)

## Context

Visual progress bars for parallel check execution during gate runs. Shows each
check's progress, ETA calculation, and cache hit indicators. Integrates with
gate runner's parallel execution.

## Steps

### 1. Define progress state model

- **Checkpoint:** CheckProgress type with name, status, progress%, duration
- **Checkpoint:** CacheStatus type: hit, miss, disabled
- **Files:** `cli/src/tui/components/feedback/types.ts`

### 2. Implement ParallelProgress component

- **Checkpoint:** Shows multiple progress bars vertically stacked
- **Checkpoint:** Each bar shows check name, progress, status
- **Checkpoint:** Completed checks collapse or show final status
- **Files:** `cli/src/tui/components/feedback/ParallelProgress.tsx`

### 3. Implement CheckProgress component

- **Checkpoint:** Single check progress bar with label
- **Checkpoint:** Animated spinner while running
- **Checkpoint:** Shows duration on completion
- **Files:** `cli/src/tui/components/feedback/CheckProgress.tsx`

### 4. Implement CacheIndicator component

- **Checkpoint:** Visual indicator for cache status
- **Checkpoint:** `⚡` for cache hit, `○` for miss, `—` for disabled
- **Checkpoint:** Tooltip-style explanation on focus
- **Files:** `cli/src/tui/components/feedback/CacheIndicator.tsx`

### 5. Add ETA calculation

- **Checkpoint:** Estimates remaining time based on completed checks
- **Checkpoint:** Shows "ETA: Xs" or "calculating..." if insufficient data
- **Files:** `cli/src/tui/components/feedback/ParallelProgress.tsx`

### 6. Integrate with gate runner

- **Checkpoint:** Gate runner emits progress events
- **Checkpoint:** Progress component subscribes to events
- **Checkpoint:** Works with both TUI and non-TUI modes
- **Files:** `core/src/gate/runner.ts`, `cli/src/commands/gate.ts`

### 7. Add tests

- **Checkpoint:** Progress bar rendering tests
- **Checkpoint:** ETA calculation tests
- **Checkpoint:** Cache indicator tests
- **Validate:** `nx test cli --testNamePattern="progress"` — passes
- **Files:** `cli/src/tui/components/feedback/**/*.test.tsx`

## Completion

- [x] All checkpoints validated
- [x] Progress updates smoothly without flicker
- [x] Task marked complete in source module
