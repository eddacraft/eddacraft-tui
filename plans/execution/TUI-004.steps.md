# Steps: TUI-004 — `anvil doctor` diagnostics

| Field      | Value                                          |
| ---------- | ---------------------------------------------- |
| Source     | [../modules/tui.aps.md](../modules/tui.aps.md) |
| Task(s)    | TUI-004 — `anvil doctor` diagnostics           |
| Created by | AI                                             |
| Status     | Complete                                       |

## Prerequisites

- [x] TUI-001 complete (component library available)

## Context

New `anvil doctor` command runs diagnostic checks and offers auto-fix for common
issues. Checks: system requirements, configuration validity, hooks installation,
file permissions.

## Steps

### 1. Define diagnostic check interface

- **Checkpoint:** DiagnosticCheck type with name, run(), fix() methods
- **Checkpoint:** DiagnosticResult with status, message, fixable flag
- **Files:** `cli/src/tui/commands/doctor/types.ts`

### 2. Implement SystemCheck

- **Checkpoint:** Verifies Node.js version >= 20
- **Checkpoint:** Verifies Bun installed (for TUI)
- **Checkpoint:** Verifies git available
- **Files:** `cli/src/tui/commands/doctor/checks/SystemCheck.ts`

### 3. Implement ConfigCheck

- **Checkpoint:** Validates .anvilrc exists and is valid JSON/YAML
- **Checkpoint:** Checks for deprecated configuration options
- **Checkpoint:** Auto-fix: create default .anvilrc if missing
- **Files:** `cli/src/tui/commands/doctor/checks/ConfigCheck.ts`

### 4. Implement HooksCheck

- **Checkpoint:** Verifies git hooks installed correctly
- **Checkpoint:** Verifies hook scripts are executable
- **Checkpoint:** Auto-fix: reinstall hooks if broken
- **Files:** `cli/src/tui/commands/doctor/checks/HooksCheck.ts`

### 5. Implement PermissionsCheck

- **Checkpoint:** Verifies write access to .anvil/ directory
- **Checkpoint:** Verifies plans directory readable
- **Files:** `cli/src/tui/commands/doctor/checks/PermissionsCheck.ts`

### 6. Create Diagnostics TUI component

- **Checkpoint:** Shows check progress with spinners
- **Checkpoint:** Shows results with pass/fail/warn status
- **Checkpoint:** Offers "Fix all" and individual fix options
- **Files:** `cli/src/tui/commands/doctor/Diagnostics.tsx`

### 7. Create doctor command

- **Checkpoint:** `anvil doctor` command registered
- **Checkpoint:** `anvil doctor --fix` auto-applies all fixes
- **Checkpoint:** Exit code reflects overall health (0=healthy, 1=issues)
- **Files:** `cli/src/commands/doctor.ts`

### 8. Add tests

- **Checkpoint:** Each check has unit tests
- **Checkpoint:** Auto-fix functions tested
- **Validate:** `nx test cli --testNamePattern="doctor"` — passes
- **Files:** `cli/src/tui/commands/doctor/**/*.test.ts`

## Completion

- [x] All checkpoints validated
- [x] Doctor identifies and fixes common issues
- [x] Task marked complete in source module

## Implementation Notes

**Completed:** 2025-12-30

**Files Created:**

- `cli/src/commands/doctor.ts` — Main doctor command
- `cli/src/tui/commands/doctor/types.ts` — DiagnosticCheck interface
- `cli/src/tui/commands/doctor/checks/SystemCheck.ts` — Node.js, git checks
- `cli/src/tui/commands/doctor/checks/ConfigCheck.ts` — .anvilrc validation
- `cli/src/tui/commands/doctor/checks/HooksCheck.ts` — Git hooks checks
- `cli/src/tui/commands/doctor/checks/PermissionsCheck.ts` — File permissions
- `cli/src/tui/commands/doctor/Diagnostics.tsx` — TUI component with spinners
- `cli/src/tui/commands/doctor/__tests__/doctor.test.tsx` — 26 tests

**Features:**

- `anvil doctor` — Run all diagnostic checks
- `anvil doctor --fix` — Auto-fix common issues
- `anvil doctor --json` — JSON output for CI/CD
- `anvil doctor --verbose` — Detailed output
- `--tui` / `--no-tui` — Force TUI or plain text mode
- Exit code 0 for healthy, 1 for issues

**Test Results:** 26 tests passing
