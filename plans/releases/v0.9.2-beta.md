# Release v0.9.2-beta — MCP 2.0 reconnect

> **Provenance:** this record was **backfilled on 2026-08-04**, one day after the
> cut, during the closeout that pruned the window from `RELEASE-PLAN.md`. It is
> not a live-at-cut record like [`v0.9.1-beta.md`](./v0.9.1-beta.md). Every field
> below is read back from the tag, the published releases, and the commit range —
> no field is reconstructed from memory. Fields the cut did not leave behind are
> marked `not recorded` rather than guessed.

| Field          | Value                                                                       |
| -------------- | --------------------------------------------------------------------------- |
| Tag            | `v0.9.2-beta` (annotated; **retagged** 2026-08-03 after the openapi fix)    |
| Status         | published                                                                   |
| lifecycleState | `published`                                                                 |
| Date           | 2026-08-03                                                                  |
| Base           | `v0.9.1-beta`                                                               |
| Cut by         | @joshuaboys (agent-operated)                                                |
| Release record | this file (backfilled 2026-08-04)                                           |
| Source SHA     | `22f6a9bec8df7008bd4b58d8b8db0ccc84497ca0` (`fix(dashboard): regen openapi for 0.9.2-beta version`) |
| Prepare commit | `fb82b61fe` — `chore(release): prepare v0.9.2-beta`                         |
| Tag object     | `0170093d93ac65b685653aa5b365acaca0b9e8c3` (annotated-tag object, not the commit) |
| Scope commit   | `09839390c` — `docs(release): scope v0.9.2-beta MCP reconnect patch`        |
| Published      | 2026-08-03T18:46:50Z private (`prerelease=false` per beta convention)       |
| Release page   | https://github.com/eddacraft/anvil-001/releases/tag/v0.9.2-beta             |
| Public release | https://github.com/eddacraft/anvil/releases/tag/v0.9.2-beta (18:46:58Z)     |
| Readiness run  | not recorded — no workflow run is associated with the tagged SHA            |
| Release run    | not recorded — see above                                                    |
| Signing run    | not recorded — see above                                                    |
| Tracking issue | not recorded                                                                |

## Theme

A patch on the v0.9 line for the **MCP 2.0 reconnect regression**. The operator
overrode assess's default `v0.10.0-beta` (a semver minor, inferred from mixed
`feat:` commits) because the product reason for the cut was a hotfix, not a new
minor theme.

## What shipped

Taken verbatim from the public changelog at the tag
(`docs/public/anvil/releases/changelog.md`):

**Fixed**

- **Codex and similar assistants work with anvil again.** After the last MCP
  update, some clients that send normal progress metadata were rejected as if
  they spoke a newer protocol, so tool lists and tool calls failed. Those
  clients connect and call tools again; broken modern requests still fail
  clearly. (`f217100af`, MCP26-013.)

**Added**

- **Prove it from `anvil start`.** On the result screen, press `t` for
  **Prove**. anvil runs a real secret check on a sample and tells you whether it
  caught the secret — a 30-second reality check, not a placeholder toast. If
  Prove cannot run here (no secret checks configured, unsupported languages), it
  says so plainly. It never pretends your editor's live save guard is working
  when only the check engine was tested.

## Range

51 commits, `v0.9.1-beta..v0.9.2-beta`, counted commit-to-commit and including
the two that arrived with the retag. The cut is dominated by the MCP26-013
fix and the ACTTUI Prove work; the remainder is planning, documentation and
governance bookkeeping.

Also carried in this range, though released on their own cadence to their own
mirror: the acknowledgements starter kit's `v1.1.0` hardening (ATTRIB-018..-024).
That kit has its own tags and its own record trail — see
[`plans/modules/acknowledgements-kit-hardening.aps.md`](../modules/acknowledgements-kit-hardening.aps.md).

## Closeout notes

- **The record was not created at cut.** `RELEASE-PLAN.md` states each shipped
  tag has an immutable record "created at cut"; this one was not, and its absence
  was found only when the `release-plan` docs surface began failing every open
  PR after the tag was created. Recorded here so the gap is visible rather than
  silently repaired.
- **The tag was moved after the first cut.** It was retagged on 2026-08-03 onto
  `22f6a9bec` after an openapi dashboard fix, so the range is 51 commits rather
  than the 49 visible before the retag was fetched. An earlier draft of this
  record cited the annotated-tag object SHA as the source SHA; the commit is
  `22f6a9bec`.
- **Three verification runs are unrecorded.** No workflow run is associated with
  the tagged SHA, so readiness/release/signing evidence cannot be cited. A
  backfilled record should not invent them.
- **Consequence still outstanding:** 357 `Merged` work items across active
  modules carry no release evidence, and none cite `v0.9.2-beta`. Flipping them
  to `Released/Shipped` needs per-item mapping from merge to tag — see the
  closeout PR for the reasoning against a bulk sweep.
