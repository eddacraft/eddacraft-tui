# Release Record: v0.7.1-beta

**Tag:** `v0.7.1-beta` (published)
**Source SHA:** `c3e55d6068134dadbfebacb90d3fd215a412ac4f`
**Published:** 2026-05-23T04:00:06Z
**Tracking issue:** [#1867](https://github.com/eddacraft/anvil-001/issues/1867)
**Release page:** https://github.com/eddacraft/anvil-001/releases/tag/v0.7.1-beta
**Release workflow run:** https://github.com/eddacraft/anvil-001/actions/runs/26322606408
**Readiness workflow run:** https://github.com/eddacraft/anvil-001/actions/runs/26322309975
**Parent tag:** `v0.7.0-beta` (`d7873161`, published 2026-05-21
11:11 UTC)
**Theme:** Activation Diagnostic Honesty (Boring Week Patch 1).
**lifecycleState:** `published` (advanced from `candidate` after
post-publish `verify.sh` + `closeout.sh` returned success on
2026-05-23).

Published release record per the
[release-record schema](../specs/2026-05-10-release-record-schema.md)
(OPMODEL-004). Authoritative for APS reconciliation: every entry
on the "APS items advanced by this tag" list below is eligible for
the cleanup-agent sweep `Merged → Released/Shipped`.

## What ships

`v0.7.1-beta` is the first Boring-Week patch tag on the `v0.7.0-beta`
daemon-working slate. It does NOT broaden the daemon-working claim
or re-open the protection-claim vocabulary — that surface is frozen
on `v0.7.0-beta`. `v0.7.1-beta` is a focused patch that closes one
user-visible regression (GH
[#1831](https://github.com/eddacraft/anvil-001/issues/1831)) plus
the structural hardening the post-release Council review flagged.

**Patch-tag claim** (applies the moment `v0.7.1-beta` is cut):
`anvil start --verify` reaches `protecting` when the intercept
daemon attests live enforcement for the current worktree on every
shipped platform (Linux, macOS, Windows), with operator-actionable
repair hints for every daemon-state that blocks promotion.

The parent tag's "Daemon-Working End-to-End Protection" claim is
preserved verbatim — `v0.7.1-beta` does NOT graduate the
`v0.7.0-beta` sit-on claim. That remains gated on Wave 5 Boring
Week observation.

**Cutting this patch tag does not reset the Wave 5 Boring Week
observation window.** That window runs on the `v0.7.0-beta`
baseline; the sit-on claim graduates on the parent tag, not on
this patch. `v0.7.1-beta` is the named patch-cadence mechanism for
Boring Week blockers per `RELEASE-PLAN.md`, not a re-tag of the
sit-on slate.

### Patch-tag-claim gates (all Merged at candidate-record creation)

- **MLP2-075** — Windows IPC parity for `query_protection_claim`
  ([PR #1836](https://github.com/eddacraft/anvil-001/pull/1836),
  merged `0a8aa57b`).
- **MLP2-051h** — `DaemonStatusV1::generated_at_unix` wire-add
  precursor (merged on `main` ahead of MLP2-051f at `a103df9d`).
- **MLP2-051f** — activation diagnostic consumes daemon
  `ProtectionClaim`
  ([PR #1840](https://github.com/eddacraft/anvil-001/pull/1840),
  merged `e1cc066a`).
- **MLP2-051f post-ship hardening** — four Council MAJORs (broken
  repair hints, silent skip-path tracing, unbounded clock skew,
  Unix slow-drip read)
  ([PR #1848](https://github.com/eddacraft/anvil-001/pull/1848),
  merged `78689cf7`).

### Freight (rides the patch tag)

These ride `v0.7.1-beta` but are not load-bearing for the patch
claim. Release notes list them under their own sections so the
claim's verifiability stays clean.

- **MLP2-069** — L4 engine IO-outage variant
  (`EngineUnavailableReason::IoError`) so transient filesystem
  errors don't surface as `engine missing`.
- **DOCGOV** — as-built source-path validation in DOCGOV closeouts.
- **`anvil uninstall`** — Scoop / WinGet install-path detection
  with tightened boundary check.
- **`anvil-run`** — manpage DIAGNOSTICS section for SIGTERM
  transient-fence behaviour (closes GH
  [#1828](https://github.com/eddacraft/anvil-001/issues/1828),
  council-deferred follow-up from the `v0.7.0-beta` pre-tag
  session).

### Deferred (named honestly in release notes)

- **MLP2-051g** — `anvil start --verify --why` verbose tier-evidence
  flag. Filed Draft; spec design locked from the activation-daemon-
  evidence council; not in `v0.7.1-beta` scope.
- **MLP2-051i** — `query_protection_claim` 500 ms timeout parity
  (MCP path still uses 2 s default). Filed Draft.
- **MLP2-051j** — Windows named-pipe client-side peer-owner SID
  validation (defence-in-depth, pre-existing pre-`v0.7.0-beta`).
  Filed Draft.

### Cross-platform parity

- **Linux x86_64 + ARM64:** all four gates green. The new
  `slow_drip_response_does_not_exceed_wall_clock_budget`
  regression test is `#[cfg(target_os = "linux")]` and runs on
  Linux CI runners only — the underlying single-deadline fix is
  shared with the macOS Unix path.
- **macOS x86_64 + ARM64:** shares the Unix `query_daemon_status_at`
  + activation `daemon_evidence` code paths with Linux. The
  single-deadline read loop, `MAX_FUTURE_CLOCK_SKEW` cap, and
  warn-level skip tracing are all platform-agnostic. Linux-only
  regression coverage is acknowledged as the post-release smoke-
  check gap noted in
  [`fix-mlp2-051f-postship-hardening.md`](../reviews/post-merge/fix-mlp2-051f-postship-hardening.md).
- **Windows x86_64:** MLP2-075 lifted `query_protection_claim` to
  named pipes; MLP2-051f's activation wire-up consumes the same
  path. The Windows single-deadline wall-clock fix in MLP2-051f
  (Copilot review round 1) brings Windows IPC budget enforcement
  to parity with the new Unix per-frame deadline.

## What changed since the parent tag

44 commits on `main` between `v0.7.0-beta` and the patch-tag
candidate SHA. The customer-relevant subset is in the
[CHANGELOG entry for `0.7.1-beta`](../../CHANGELOG.md).
The remaining commits are docs / APS / spec maintenance per
`ENGINEERING-HISTORY.md` conventions.

## Tracking issue

To be opened at tag-cut time per the
[`v0.7.0-beta` runbook](../../docs/runbooks/v0.7.0-beta-release-runbook.md)
shape, naming this record + the CHANGELOG entry as the
authoritative inputs. The release engineer fills in:

- `Source SHA` once `tag.sh` runs against the green main SHA.
- `Published` once GitHub Release + Scoop / Homebrew / WinGet
  publishes succeed.
- `lifecycleState: "published"` transition after post-publish
  verification (see runbook §13–§15).

## APS items advanced by this tag

Recorded here for post-publish APS reconciliation. The cleanup
agent advances `Merged → Released/Shipped` for each entry once
`lifecycleState: "published"` is set.

- MLP2-075 (currently `Merged`)
- MLP2-051f (currently `Merged`)
- MLP2-051h (currently `Merged`)
- MLP2-069 (currently `Merged`)
- DOCGOV-related work (cleanup agent reconciles per-task)

Filings without code in this tag (MLP2-051g / -051i / -051j) stay
at their `Draft` status. They're explicitly out of scope.

## Distribution surfaces

Same matrix as `v0.7.0-beta`:

- **GitHub Release:** binaries for Linux x86_64 / ARM64, macOS
  x86_64 / ARM64, Windows x86_64.
- **Scoop bucket:** `eddacraft/anvil` manifest published via the
  `scoop` job in `.github/workflows/release.yml`. Triggered by
  cargo-dist's release pipeline once the tag is pushed; the job
  bumps the manifest in the `eddacraft/scoop-anvil` bucket.
- **Homebrew tap:** `eddacraft/anvil` formula bumps via
  `scripts/release/bump-homebrew.sh` (DISTRIB-003 auto-bump path,
  invoked from the release workflow after the GitHub Release
  artefacts publish).
- **WinGet:** manifest bump via the `winget` job in
  `.github/workflows/release.yml`.

## Council review

`v0.7.1-beta` is a patch tag; the pre-tag Council session runs
the standard release-tier review against this record + the
CHANGELOG entry. The MLP2-051f hardening PR already passed a full
5-persona post-merge Council review (2026-05-22, 0 CRITICAL /
11 MAJOR / 14 MINOR / 5 NIT, all pre-release MAJORs addressed in
[PR #1848](https://github.com/eddacraft/anvil-001/pull/1848)), so
the patch-tag Council session is expected to be a focused
verify-of-record rather than a fresh-eyes audit.

## Risk + rollback

- **Risk:** the activation surface now performs an IPC round-trip
  on every `verify()` invocation when at least one MCP client is
  at `RestartHandshakeVerified`. The 500 ms budget bounds the cost;
  a hung daemon does not stretch `verify` measurably beyond the
  pre-`v0.7.1-beta` baseline.
- **Rollback path:** the `daemon_evidence` module is invoked from
  a single call site in
  `activation::diagnostic::verify_with_home`. Deleting that call
  reverts to the `v0.7.0-beta` behaviour without touching any wire
  shape. The `generated_at_unix` field is wire-additive and
  consumers tolerate `0` as "no anchor".
- **No JSON-schema-consuming fixture revert needed.** The
  `ActivationDiagnostic.daemon_attestation` field is internal-
  surface; no `--json` consumer outside the binary depends on it.

## References

- Parent tag record:
  [`plans/releases/v0.7.0-beta.md`](v0.7.0-beta.md).
- Activation-daemon-evidence spec:
  [`plans/specs/2026-05-21-activation-daemon-evidence-wireup.md`](../specs/2026-05-21-activation-daemon-evidence-wireup.md).
- Post-merge hardening plan:
  [`plans/reviews/post-merge/fix-mlp2-051f-postship-hardening.md`](../reviews/post-merge/fix-mlp2-051f-postship-hardening.md).
- Original user reports (Windows + Scoop): GH
  [#1831](https://github.com/eddacraft/anvil-001/issues/1831).
