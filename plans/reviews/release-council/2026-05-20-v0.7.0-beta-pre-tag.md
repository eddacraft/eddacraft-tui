# v0.7.0-beta pre-tag release council — passes 1 + 2

**Date:** 2026-05-20 (pass 1 + pass 2, same day)
**Candidate SHA:** TBD (not yet captured — see "Re-run §1" note below).
**Council reviewers (pass 1):** kernel-maintainer, security-analyst,
operations-reviewer.
**Council reviewers (pass 2):** kernel-maintainer (medium + confirmed-bug),
pragmatic-lead (low-severity bulk).
**Input audit:** `plans/audits/2026-05-19-clawpatch-v0.7.0-beta.json` —
**invalidated** by PR #1732 once it merges (ADV-007). A re-sweep is required
against the new `main` SHA before the tag cuts.
**Status:** **Complete (per the §2 finding-coverage contract).** Across both
passes, every one of the 64 tracked clawpatch findings has an explicit
ship / fix-before-tag / defer-with-issue verdict. 3 DeepSec issues + 1 INTD
wire-up issue + 1 ADV finding from PR #1732 were also triaged.

## Verdicts produced this pass

| Item     | Subject                                                | Verdict             | Owner / follow-up                  |
| -------- | ------------------------------------------------------ | ------------------- | ---------------------------------- |
| CLAWP-001 | Vacuous `--insecure-skip-verify` integration tests    | fix-before-tag      | **PR #1732 (open, mergeable)**     |
| CLAWP-002 | `Engine::eval` collapses multi-result Rego queries    | **ship**            | No action — single-expression contract holds (zero external callers, all in-module tests). |
| CLAWP-003 | `IpcCommand::QueryStatus` wire/in-memory asymmetry    | **ship**            | No action — daemon returns a deliberate redirect error (`ipc.rs:3018`); documented divergence. |
| CLAWP-004 | Win32 named-pipe EOF surfaces as OS error not Ok(0)   | defer-with-issue    | **#1736** filed; ~15-line fix, platform-confined, no current caller relies on `Ok(0)` contract. |
| CLAWP-005 | Mid-edit contract fixtures trapped in test target     | defer-with-issue    | **#1737** filed; ~400-line move, must precede `anvil-rmcp` (RTAI-006). |
| ADV-009 | Sidecar path silently drops `--insecure-skip-verify`   | defer-with-issue    | **#1735** filed; same operator-config-silent-default class as #1671 / PR #1721. |
| #1672   | DeepSec — token revocation leaves refresh sessions     | defer-with-issue    | Out-of-scope for binary tag; route to `anvil-api-v0.6.3`. Triage posted. |
| #1673   | DeepSec — private docs gate accepts any valid licence  | defer-with-issue    | Out-of-scope for binary tag; route to `docs-shell-v0.3.1`. Triage posted. |
| #1674   | DeepSec — IPC clients can mint trusted lineage tags    | **ship-with-doc**   | Need new `docs/runbooks/v0.7.0-beta-security-note.md` + CHANGELOG "Known gaps" + MLP-014 APS link. |
| #1722   | INTD-015 — Fanout cross-session policy unreachable     | defer-with-issue    | File APS task in `intercept-daemon.aps.md` (Blocked pending design pass) + release-note line. |

## What changed in the repo as a result of this pass

- **Issues filed:** #1735 (ADV-009), #1736 (CLAWP-004), #1737 (CLAWP-005).
- **Issues commented:** #1672, #1673, #1674, #1722 — each carries the
  council verdict + reachability evidence + concrete next actions.
- **APS module updates:** `plans/modules/clawpatch-pre-tag-v0.7.0-beta.aps.md`
  cross-references the new GH issue numbers on CLAWP-004 / -005 and notes
  the ship verdicts on CLAWP-002 / -003.

## Tag-blocking obligations still open

> **Note (2026-05-20 post-pass-2):** Items 4 and 5 below capture the pass-1
> intended tracking destinations (`MLP-014` APS link, `intercept-daemon.aps.md`).
> The actual landing destinations after `intercept-daemon.aps.md` was confirmed
> archived at 16/16 Complete are `MLP2-070` and `MLP2-071` in
> [`plans/modules/multilayer-protection-v2.aps.md`](../../modules/multilayer-protection-v2.aps.md)
> Group P; see the "Tag-blocking obligations remaining" section further down
> for the updated wording. The same applies to the `#1674` / `#1722` rows in
> the verdict table above.

Before `v0.7.0-beta` can be cut, the following must land:

1. **PR #1732 merge** — closes CLAWP-001 (the only High in the sweep) +
   CLAWP-029 (#1647) + CLAWP-030 (#1649). All required CI green.
2. **§1 clawpatch sweep re-run** against the new `main` SHA after PR #1732
   merges (ADV-007 — current audit JSON is invalidated by the test rewrite +
   doc fixes).
3. **Verdicts for the remaining 59 clawpatch findings** —
   CLAWP-006..028, -031..-064. Of these:
   - 7 have GH issues already (#1642-1651 set: CLAWP-006/007/008/010/031/
     032/037/038/047/048; verify each maps to the right CLAWP-NNN before
     accepting "tracked = triaged").
   - ~52 have neither GH issue nor council verdict. The runbook §2
     contract is "ship / fix / defer-with-issue" per finding. Realistic
     batch path: group by severity (1 high, 25 medium, 38 low) and by
     triage (2 confirmed-bug, 4 contract-mismatch, 2 docs-gap, 10 risk,
     46 test-gap). The 46 test-gaps are the bulk and most defer-eligible.
4. **For #1674 ship-with-doc verdict:**
   - Write `docs/runbooks/v0.7.0-beta-security-note.md` (~40 lines, H1-H4
     structure mirroring `v0.6.0-beta-security-note.md`).
   - Confirm MLP-014 (daemon-mint-tag) has a tracked APS item; file it if
     absent.
   - Add "Known gaps" CHANGELOG entry under `[0.7.0-beta]` linking the
     security note.
5. **For #1722 defer-with-issue verdict:** file the APS task in
   `plans/modules/intercept-daemon.aps.md` (Blocked status; design-pass
   gate documented) and add the release-note line about `allow_cross_session`
   being inert in v0.7.0-beta.

## Pass 2 verdicts (closes the §2 finding-coverage contract)

Pass 2 (same date, 2026-05-20) triaged the remaining 59 clawpatch findings
across two reviewers running in parallel:

### Elevated fix-before-tag (NEW from pass 2)

| Item | Subject | Tracked in | Fix shape |
|------|---------|------------|-----------|
| **CLAWP-008** | `scan_artifact_json` registry-load panic outside `catch_unwind` — breaks `release-napi` profile contract | **#1650 (elevated via comment)** | `crates/anvil-checks-napi/src/lib.rs`: extend `catch_unwind` to cover `:170`. ~+5/-5 lines. |
| **CLAWP-028** | Confirmed off-by-one in capped welcome scan (`files_scanned` reports 501 against a stated cap of 500) | **#1741 filed** | `crates/anvil-checks/tests/discovery_repro.rs` + `crates/anvil-checks/src/welcome.rs`: check-before-increment. ~3 lines per file. |

### Ship (no action — pass 2)

| Item | Subject | Rationale |
|------|---------|-----------|
| CLAWP-016 | Idempotence guard | Workdir snapshot before+after is the actual contract; exit status is intentionally discarded. |
| CLAWP-018 | Resolution-chain coverage absent from integration entrypoint | Covered by CLAWP-001 PR #1732 rewrite; integration file documents the new location. |
| CLAWP-020 | Claude Code missing-type test | Audit misread verify semantics — the test asserts the correct failure mode. |
| CLAWP-026 | Same-name symbol collision suppression | `same_name_different_file_public_symbol_still_flags_new_export` at `:719` already covers it; audit stale. |
| CLAWP-036 | Blocking-rule barrier zombie risk | Barrier inside Tokio task; runtime tear-down on panic cancels the task. |

### Defer with individual GH issue (NEW from pass 2 — 15 issues filed)

CLAWP-009 → #1743, CLAWP-011 → #1744, CLAWP-012 → #1745, CLAWP-013 → #1746,
CLAWP-014 → #1747, CLAWP-015 → #1748, CLAWP-017 → #1749, CLAWP-019 → #1750,
CLAWP-021 → #1751, CLAWP-022 → #1752, CLAWP-023 → #1753, CLAWP-024 → #1756
(security-adjacent — `anvil_suppress` mutation containment), CLAWP-025 → #1754,
CLAWP-027 → #1755, CLAWP-033 → #1742 (CI flake source, broken out from batch).

### Defer in batch tracker — **#1740 filed**

28 low-severity test-hygiene items consolidated under one tracking issue:
CLAWP-034, -035, -039, -040, -041, -042, -043, -044, -045, -046, -047, -048,
-049, -050, -051, -052, -053, -054, -055, -056, -057, -058, -059, -060, -061,
-062, -063, -064.

Rationale: none of these affect the daemon-working claim, the IPC surface,
the signature-verification / baseline-policy / auth-gate / air-gap surfaces,
or any operator-visible behaviour under Boring Week sit-on. Cluster as a
post-tag cleanup sprint.

### Defer with pre-existing GH issue (no new action — confirmed in pass 2)

CLAWP-006 → #1646, CLAWP-007 → #1648, CLAWP-010 → #1645, CLAWP-031 → #1642,
CLAWP-032 → #1644, CLAWP-037 → #1643, CLAWP-038 → #1651.

## Full verdict roll-up

Across both passes:

- **Fix-before-tag:** 3 items (CLAWP-001, -008, -028).
- **Ship (no action):** 9 items (CLAWP-002, -003, -016, -018, -020, -026, -029, -030, -036).
- **Defer with individual GH issue:** 21 items (CLAWP-004 → #1736, -005 → #1737, -006 → #1646, -007 → #1648, -009 → #1743, -010 → #1645, -011 → #1744, -012 → #1745, -013 → #1746, -014 → #1747, -015 → #1748, -017 → #1749, -019 → #1750, -021 → #1751, -022 → #1752, -023 → #1753, -024 → #1756, -025 → #1754, -027 → #1755, -033 → #1742, -031 → #1642, -032 → #1644, -037 → #1643, -038 → #1651).
- **Defer in batch tracker #1740:** 28 items (CLAWP-034..-064 minus the breakouts above).

Plus the four GH-issue-shaped items triaged in pass 1: #1672, #1673 (route off-tag), #1674 (ship-with-doc), #1722 (defer with APS task).
Plus ADV-009 from PR #1732 council session: #1735.

## Tag-blocking obligations remaining

After PR #1732 merges, the residual tag-blocking work is:

1. **§1 clawpatch sweep re-run** against the new `main` SHA (ADV-007 — the
   2026-05-19 audit JSON is invalidated by #1732's bits).
2. **CLAWP-008 patch** — extend `catch_unwind` in `anvil-checks-napi`.
3. **CLAWP-028 patch** — fix the welcome-scan off-by-one in `welcome.rs`.
4. ~~**For #1674 ship-with-doc verdict:** write
   `docs/runbooks/v0.7.0-beta-security-note.md` (~40 lines) + confirm MLP-014
   has a tracked APS item + add CHANGELOG "Known gaps" entry.~~ **Satisfied
   2026-05-20** by Chunk A pre-tag obligation docs:
   `docs/runbooks/v0.7.0-beta-security-note.md` shipped (§M1 covers the
   lineage-mint surface); CHANGELOG.md `[Unreleased]` gains a "Known gaps"
   subsection linking the note; MLP2-070 filed in
   `plans/modules/multilayer-protection-v2.aps.md` (Group P) as the
   tracked follow-up since MLP-014 itself is Complete in the archived
   parent module.
5. ~~**For #1722 defer verdict:** file the APS task in
   `plans/modules/intercept-daemon.aps.md` + add the release-note line about
   `allow_cross_session` being inert.~~ **Satisfied 2026-05-20** by Chunk A:
   MLP2-071 filed in `plans/modules/multilayer-protection-v2.aps.md`
   (Group P, status Blocked, pending the cross-session-attribution design
   pass) — filed in MLP2 rather than `intercept-daemon.aps.md` because
   INTD is archived at 16/16 Complete; MLP2 is the active home for daemon
   integration debt. CHANGELOG "Known gaps" carries the operator-facing
   line about `allow_cross_session` being inert in `v0.7.0-beta`.

Items 2–3 are the only docs-unrelated residual tag-blocking work; items 4–5
landed in the same docs PR. None requires re-running the council unless
their fix surfaces meaningful new code.

## Provenance

- Reviewer agents: `kernel-maintainer` (CLAWP-002..005),
  `security-analyst` (#1672/#1673/#1674), `operations-reviewer` (#1722).
- Input audit JSON: `plans/audits/2026-05-19-clawpatch-v0.7.0-beta.json`.
- Source PR for CLAWP-001 close: PR #1732 (`fix/clawp-001-test-rewrites-doc-fixes`).
- Cross-reference: `plans/modules/clawpatch-pre-tag-v0.7.0-beta.aps.md`,
  `docs/runbooks/v0.7.0-beta-release-runbook.md`.
