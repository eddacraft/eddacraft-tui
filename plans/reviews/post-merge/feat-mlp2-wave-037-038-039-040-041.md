# Post-merge: feat/mlp2-wave-037-038-039-040-041

PR: #NNN
Branch: `feat/mlp2-wave-037-038-039-040-041`
APS: MLP2 (multilayer-protection-v2), Group H (Hook + config completion)
Merged: <!-- filled by cleanup agent -->
Verified: <!-- filled by cleanup agent -->

## Steps

- [x] Advance MLP2-037 status `In Progress → Merged` in
  `plans/modules/multilayer-protection-v2.aps.md`, add Evidence block
  with PR + merge SHA. (agent: yes)
- [x] Advance MLP2-038 status `In Progress → Merged` in
  `plans/modules/multilayer-protection-v2.aps.md`, add Evidence block
  with PR + merge SHA. (agent: yes)
- [x] Advance MLP2-039 status `In Progress → Merged` in
  `plans/modules/multilayer-protection-v2.aps.md`, add Evidence block
  with PR + merge SHA. (agent: yes)
- [x] Advance MLP2-040 status `In Progress → Merged` in
  `plans/modules/multilayer-protection-v2.aps.md`, add Evidence block
  with PR + merge SHA. (agent: yes)
- [x] Advance MLP2-041 status `In Progress → Merged` in
  `plans/modules/multilayer-protection-v2.aps.md`, add Evidence block
  with PR + merge SHA. (agent: yes)
- [x] Bump Group H done/total `0/5 → 5/5` and mark Group H Complete.
  (agent: yes)
- [x] Update the module's overall counter (now 48/68 after subsequent waves; this wave's +5 contribution is rolled into the current total). (agent: yes)
- [x] Update `plans/index.aps.md` MLP2 progress line + add wave-1G
  evidence sentence naming the PR, merge SHA, and the five tasks
  Merged. (agent: yes)
- [ ] Remove `#![allow(dead_code)]` from
  `crates/anvil-cli/src/config_view.rs` once `commands/gate.rs` /
  `commands/init.rs` adopt `GateConfigView` / `InitConfigView` in their
  follow-up. (human required — gated on the consumer-adoption PRs)
- [ ] Council follow-up: add a boundary test for `commit_is_witnessed`
  (Council MINOR; substring match safety pin). (agent: yes)
- [ ] Council follow-up: consider extracting the duplicated `json_kind`
  helper between `anvil-cli/src/config_view.rs` and
  `anvil-policy/src/config_view.rs` (Council NIT). (human required)
- [ ] Coordinate with the parallel wave 1D branch
  (`feat/mlp2-wave-058-049-046-012`) and the in-flight Group G work
  (`feat/mlp2-032-034-baseline-identity`) — files do not overlap, but
  the MLP2 counter update has to be linearised in the order PRs land.
  (human required)
- [x] Clean up the parallel-agent worktree at
  `~/Projects/src/anvil-001/.claude/worktrees/agent-af3e15bcb8ea63d63`
  (locked + ce7ecbab commit already cherry-picked into wave-1G).
  (agent: yes)
- [x] Council session: `council-e8633cef` (quick, converged,
  2 MAJOR + 2 MINOR fixed in `1152188b`).

## Evidence

- Tests: 1412 anvil-cli (+24 new) / 125 anvil-hook (+1 new) / 75
  anvil-policy (+8 new); `cargo test --workspace` exit 0.
- Lint: `cargo fmt --check` and
  `cargo clippy --workspace --all-targets -- -D warnings` clean.
- Council: session `council-e8633cef`, 6 findings (0 critical /
  2 major / 3 minor / 1 nit); 2 major + 2 minor closed by `1152188b`;
  3 follow-ups carried forward in this file.
