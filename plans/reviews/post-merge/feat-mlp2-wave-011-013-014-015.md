# Post-merge: feat/mlp2-wave-011-013-014-015

PR: #NNN
Branch: `feat/mlp2-wave-011-013-014-015`
APS: MLP2 (multilayer-protection-v2), Group B (Witness chain extensions)
Merged: <!-- filled by cleanup agent -->
Verified: <!-- filled by cleanup agent -->

## Steps

- [ ] Advance MLP2-011 status `In Progress → Merged` in
  `plans/modules/multilayer-protection-v2.aps.md`. (agent: yes)
- [ ] Advance MLP2-013 status `In Progress → Merged`. (agent: yes)
- [ ] Advance MLP2-014 status `In Progress → Merged`. (agent: yes)
- [ ] Advance MLP2-015 status `In Progress → Merged`. (agent: yes)
- [x] Bump Group B counter to `5/5`. (-012 already Merged before this
  wave; -011/-013/-014/-015 completed the remaining four items.)
  (agent: yes)
- [x] Module/index counters reconciled by the 2026-05-15 full MLP/MLP2
  Council audit. Wave 1I initially advanced progress, then MLP2-016 and
  MLP2-048 were reopened and Group M was added; current authority is
  35/66 in `plans/modules/multilayer-protection-v2.aps.md` and
  `plans/index.aps.md`. (agent: yes)
- [ ] Update `plans/index.aps.md` MLP2 progress lines (3 sites) +
  add wave-1I evidence sentence naming PR, merge SHA, and the four
  tasks Merged. (agent: yes)
- [ ] Council follow-up: document the rules_sha forward-compat plan
  for the future rule-engine wiring task. Council MINOR — the
  empty-rules digest written by MLP2-014 will differ from the real
  digest once the rule-engine is wired; the L4 registry needs to
  recognise both variants (or rely on OnNoWitness=Allow during the
  transition). (human required — this is an APS spec change on the
  future rule-engine task, not a code change.)
- [ ] Clean up the three agent worktrees once the wave is merged:
  - `~/Projects/src/anvil-001/.claude/worktrees/agent-a0eb51414212df671` (MLP2-011)
  - `~/Projects/src/anvil-001/.claude/worktrees/agent-acac88e2863db77ce` (MLP2-013)
  - `~/Projects/src/anvil-001/.claude/worktrees/agent-a6079991e1fbe1b1b` (MLP2-014)
  All locked; safe to remove post-merge since the commits are
  cherry-picked into wave-1I. (agent: yes)
- [ ] Remove the wave worktree
  `~/Projects/src/anvil-001.feat-mlp2-wave-011-013-014-015` via
  `wt remove`. (agent: yes)
- [ ] Council session: `council-8c8842cf` (quick, converged,
  1 MAJOR + 2 NIT + 1 MINOR fixed in `bbeb1262`; 1 MINOR carried
  forward as the rules_sha follow-up above).

## Evidence

- Tests: anvil-witness 47 + 2 integration (was 33 + 1; +14 new),
  anvil-baseline 57 (was 53; +4), anvil-cli 1464 (was 1460; +4).
  Total +22 wave-new. `cargo test --workspace` exit 0.
- Lint: `cargo fmt --check` and
  `cargo clippy --workspace --all-targets -- -D warnings` clean.
- Council: session `council-8c8842cf`, 5 findings (0 critical /
  1 major / 2 minor / 2 nit); 1 major + 2 nit + 1 minor closed by
  `bbeb1262`; 1 minor (rules_sha forward-compat plan) carried
  forward in this file.
- Regression: MLP2-011 made `verify_chain` linear-only. The
  follow-up commit `fix(wave-1i): migrate verify_chain callers to
  verify_chain_dag` migrated 3 production sites; the Council
  review caught a 4th (in `anvil-baseline::save_with_genesis`)
  which was fixed in the same Council commit.
