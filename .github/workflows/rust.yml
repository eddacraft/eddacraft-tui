name: Rust

on:
  push:
    branches: [main, dev, 'rust-*']
    paths:
      - 'crates/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain.toml'
      - '.github/workflows/rust.yml'
  pull_request:
    paths:
      - 'crates/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain.toml'
      - '.github/workflows/rust.yml'

permissions:
  contents: read

concurrency:
  group: rust-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  # Pinned so cache invalidation is intentional when we bump this.
  CARGO_LLVM_COV_VERSION: '0.6.14'

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions-rust-lang/setup-rust-toolchain@2b1f5e9b395427c92ee4e3331786ca3c37afe2d7 # v1
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          shared-key: rust-ci
      - run: cargo check --workspace --all-targets

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions-rust-lang/setup-rust-toolchain@2b1f5e9b395427c92ee4e3331786ca3c37afe2d7 # v1
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          shared-key: rust-ci

      - name: Install OPA
        uses: open-policy-agent/setup-opa@950f159a49aa91f9323f36f1de81c7f6b5de9576 # v2.3.0
        with:
          # Pinned to DEFAULT_OPA_VERSION in packages/anvil/policy/src/opa-binary-manager.ts
          version: v0.60.0

      - name: Install Regal
        uses: open-policy-agent/setup-regal@33a142b1189004e0f14bf42b15972c67eecce776 # v1
        with:
          version: latest

      - name: Run OPA policy tests
        run: opa test --verbose policies/fixtures/

      - name: Lint policies with Regal
        run: regal lint --format github policies/fixtures/

      - name: Cache cargo-llvm-cov
        id: cache-llvm-cov
        uses: actions/cache@1bd1e32a3bdc45362d1e726936510720a7c30a57 # v4
        with:
          path: ~/.cargo/bin/cargo-llvm-cov
          # Version-scoped key so bumping CARGO_LLVM_COV_VERSION below
          # invalidates the cache deterministically rather than serving a
          # stale pre-upgrade binary forever.
          key: cargo-llvm-cov-${{ runner.os }}-${{ env.CARGO_LLVM_COV_VERSION }}

      - name: Install cargo-llvm-cov
        if: steps.cache-llvm-cov.outputs.cache-hit != 'true'
        # TODO: swap to taiki-e/install-action@<verified-sha> for faster install
        run: cargo install cargo-llvm-cov --locked --version ${{ env.CARGO_LLVM_COV_VERSION }}

      - name: Install cargo-nextest
        uses: taiki-e/install-action@055f5df8c3f65ea01cd41e9dc855becd88953486 # v2.75.18
        with:
          tool: cargo-nextest

      - name: Run tests with coverage (nextest)
        run: |
          cargo llvm-cov nextest --workspace --json --output-path coverage-rust.json
          cargo llvm-cov report --summary-only | tee coverage-rust-summary.txt

      # nextest does not run doctests, so we run them separately to preserve
      # the same test surface as the pre-RUSTNX-002 `cargo test` invocation.
      - name: Run doctests
        run: cargo test --doc --workspace

      - name: Rust coverage summary
        if: always()
        shell: bash
        run: |
          {
            echo "### Rust Coverage"
            echo
            echo '<details><summary>Per-file / workspace summary (cargo llvm-cov)</summary>'
            echo
            echo '```'
            if [ -f coverage-rust-summary.txt ]; then
              cat coverage-rust-summary.txt
            else
              echo "(coverage summary not produced — cargo llvm-cov step failed)"
            fi
            echo '```'
            echo '</details>'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload Rust coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage-report-rust
          path: |
            coverage-rust.json
            coverage-rust-summary.txt

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions-rust-lang/setup-rust-toolchain@2b1f5e9b395427c92ee4e3331786ca3c37afe2d7 # v1
        with:
          components: clippy
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          shared-key: rust-ci
      - run: cargo clippy --workspace --all-targets -- -D warnings

  format:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions-rust-lang/setup-rust-toolchain@2b1f5e9b395427c92ee4e3331786ca3c37afe2d7 # v1
        with:
          components: rustfmt
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          shared-key: rust-ci
          save-if: 'false'
      - uses: actions-rust-lang/rustfmt@4066006ec54a31931b9b1fddfd38f2fdf2d27143 # v1

  cross-compile:
    name: Cross (${{ matrix.target }})
    # Only run cross-compilation on PRs targeting main or pushes to main.
    # Feature branch PRs get check + test + clippy + format only.
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name ==
      'pull_request' && github.base_ref == 'main')
    runs-on: ${{ matrix.os }}
    needs: check
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            can_test: true
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            can_test: false
          - target: x86_64-apple-darwin
            os: macos-latest
            can_test: false
          - target: aarch64-apple-darwin
            os: macos-latest
            can_test: true
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            can_test: true
          - target: aarch64-pc-windows-msvc
            os: windows-latest
            can_test: false
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions-rust-lang/setup-rust-toolchain@2b1f5e9b395427c92ee4e3331786ca3c37afe2d7 # v1
        with:
          target: ${{ matrix.target }}

      - name: Install cross-compilation tools
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update && sudo apt-get install -y gcc-aarch64-linux-gnu
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> $GITHUB_ENV

      - name: Build
        run: cargo build --workspace --target ${{ matrix.target }}

      - name: Smoke test
        if: matrix.can_test
        run: cargo test --workspace --target ${{ matrix.target }} -- --test-threads=1
