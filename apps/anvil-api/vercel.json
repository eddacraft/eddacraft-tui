{
  "buildCommand": "cd $(git rev-parse --show-toplevel) && npx pnpm --filter @eddacraft/transactional build && cd apps/anvil-api && npx tsc",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
