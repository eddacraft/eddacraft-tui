
anvil initialised successfully.
  Config:    .anvil.yaml
  Plans:     plans/
  Checks:    secret-detection, import-boundaries, antipattern-scan
  Gitignore: .gitignore

First scan
────────────────────────────────────────
  No files matched this first scan's language coverage — nothing was scanned.

  Try one of:
    • `anvil tutorial` for a guided walkthrough
    • `anvil watch` once you've added some code
    • `anvil check --all` to scan the whole project later
    • `anvil auth login` to authenticate (unlocks gate-evaluated checks)

ACTIVATION
  state: ready_restart_required
  Daemon not reachable — protection cannot graduate until the intercept daemon answers this worktree.
  meaning: An anvil MCP entry is present, but the local intercept daemon is not reachable. Protection cannot graduate until the daemon answers this worktree.
  config: valid
  mcp:
    Cursor: restart_handshake_verified (pending restart)
    Claude Code: restart_handshake_verified (pending restart)
  watch: not_requested
  baseline: absent
  next: no intercept daemon is answering for this worktree, so another editor restart will not help; run `anvil start` in a real terminal (not piped) to auto-start the daemon — for headless recovery use `anvil intercept start --foreground` — then re-run `anvil start --verify`.
  install:
    Cursor: installed at <HOME>/.cursor/mcp.json (fresh)
    Claude Code: installed at <HOME>/.claude.json (fresh)
  rule modes: public-api-expansion=warn, new-dependency-introduction=warn, cross-layer-violation=warn, privilege-expansion=warn
  rule source: .anvil.yaml
  daemon: not started (--no-daemon); save-time validation uses the scoped fallback.
  worktree: project config may be written here; run `git init` (then re-run `anvil start`) or `anvil workspace register <path>` before protection can attach.

verify:
  state: ready_restart_required
  active layers:
    - L0 mcp pre-write (pending — restart required)
  recipe (try this now — triggers `secret-detection`):
    1. echo 'const KEY = "AKIAQRSTUVWXYZ123456";' >> .anvil-smoke-test.ts
    2. expect: `anvil check .anvil-smoke-test.ts` reports a secret-detection finding
    3. rm .anvil-smoke-test.ts when done
  AI tools detected: claude-code, cursor

  Anonymous usage telemetry
  anvil sends at most one anonymous beacon per day so active installs
  and feature adoption can be counted. It only ever contains:
    - schema version
    - anvil version
    - install method
    - platform
    - release channel
    - flag snapshot version
    - feature usage counts
    - anonymous install id (random; derived from nothing)
  Never: paths, repository names, arguments, hostnames, or emails.
  Turn it off any time: `anvil telemetry off`
  (or set ANVIL_TELEMETRY=off or DO_NOT_TRACK=1). Audit it: `anvil telemetry`.
