[package]
name = "eddacraft-anvil-intercept-macos"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "macOS-only helpers for the Anvil intercept daemon"

[lib]
name = "anvil_intercept_macos"

[target.'cfg(target_os = "macos")'.dependencies]
libc = { version = "0.2", features = ["extra_traits"] }
