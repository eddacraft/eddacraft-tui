# Local-dev override file checked into the repo by mistake — should
# trip SURFENV-003 because the values point at production.
DATABASE_URL=postgres://prod-db.acme.io/app
FEATURE_FLAGS_ENV=production
SECRET_PROD=ABCDEFGHIJKLMNOPQRSTUVWXYZ
# @anvil-ignore SURFENV-003 -- intentional staging-replay scenario
LEGACY_HOST=api.production.acme.io
# Should NOT fire — staging short-circuit:
SECONDARY_HOST=api.staging.acme.io
